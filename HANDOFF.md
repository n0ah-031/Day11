# 핸드오프: 취합 프로그램 (F1 양식 생성 · F2 엑셀 취합 · F3 한글 병합)

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-08-04 (6차 갱신) |
| 브랜치 | `claude/handoff-work-progress-f416b0` (이전: `claude/day11-handoff-continuation-2021a1`, `claude/docs-aggregation-program-31c2ae`) |
| 기준 문서 | [docs/PRD_v2.0.md](docs/PRD_v2.0.md) (최신), [docs/취합기능_기술명세_v0.1.md](docs/취합기능_기술명세_v0.1.md), [docs/생성기능_기술명세_v0.1.md](docs/생성기능_기술명세_v0.1.md), [docs/design.md](docs/design.md) |
| 상태 | **F1 생성 / F6 등록 → F2 취합 → 다운로드 → 이력이 화면에서 이어집니다.** F2 취합 + F4-1 인증 + F4-2 이력 + F4-3 Admin 콘솔 구현, Supabase 영속화 연결. **F1 AI 양식 생성 전 구간 동작**(대화형 수정 F1-7 포함). **F6 기존 양식 인지·등록 전 구간 동작**(첨부·의도 판정·구조 인지·무변경 등록·취합 연결, 실제 업무 양식으로 브라우저 실측 완료). **F3 hwpx 병합 전 구간 동작**(SDK 전환 게이트 판정: 전환 불필요) |
| 실제 파일 검증 | 엑셀 **2계열** — 지사별 자체점검 8개, 분기별 실적 12개(이슈 6,884→571건). F6 인지도 같은 파일로 실측(§4). hwpx는 감사 처분요구서 3개 + 나머지 계열은 합성 fixture. 근거는 모두 §4 |
| 남은 것 | 마이그레이션 baseline · hwpx 다른 계열 실제 파일 확인 (§5, 둘 다 외부 조건이 풀려야 가능) |

---

## 0. 먼저 할 일

### 0.1 환경 확인

**막혀 있는 것은 없습니다.** 마이그레이션은 `20260804060000`(form_templates.source)까지
원격에 반영돼 있고 적용 기록도 맞습니다(`supabase migration list --linked`로 확인).

```bash
python3 test_aggregate.py && python3 test_server.py && python3 test_hwpx.py \
  && python3 test_formgen.py && python3 test_auth.py
```

9개 · 13개 · 5개(확인 항목 14개가 ✓로 찍힙니다) · 9개 · 14개 시나리오가 전부
통과해야 정상입니다. 앞의 넷은 네트워크 없이 돌고, `test_auth.py`는 실제 Supabase가
필요하며 끝나면 **DB가 계정 1건(admin01)·감사 로그 0건·버킷 양쪽 비어 있는 상태**여야
합니다.

이어서 할 일은 §5입니다.

### 0.2 스키마를 바꿀 때

**`supabase db push`는 쓸 수 없습니다** — 원격 전용 베이스라인(`0001`·`0002`)이 로컬에 없어
이력 검증에서 거부됩니다(§5의 마이그레이션 baseline 항목). `migration repair --status reverted`로
우회하면 실제 적용된 마이그레이션을 적용 안 된 것으로 기록하게 되므로, 대시보드 SQL 에디터로
적용하는 쪽을 택했습니다(기본 스키마도 그렇게 만들어졌습니다).

절차는 이렇습니다 — 마이그레이션 파일을 쓰고, **사용자에게 적용을 부탁하고**(원격 DB
변경은 미리 물어봅니다), 반영을 확인한 뒤 기록만 맞춥니다.

```bash
pbcopy < supabase/migrations/<파일>.sql     # → 대시보드 SQL 에디터에 붙여 Run
supabase migration repair --status applied <타임스탬프>
```

### 0.3 `.env` 확인

`.env`는 `.gitignore`에 등록되어 **커밋되지 않습니다.** 같은 머신에서 이어받으면 이미 있고, 다른 머신이면 새로 만들어야 합니다.

```
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5-mini
SUPABASE_URL=https://cfchnprkizcmfymlezyo.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
```

- **키를 `.env` 외의 파일에 적지 마세요.** 이전에 템플릿 파일(`.env.example`)에 실제 키를 적어 커밋 직전까지 간 사고가 있었고, 그래서 템플릿 파일 자체를 삭제했습니다. 키 파일은 `.env` 하나뿐입니다.
- Supabase 키는 `supabase projects api-keys --project-ref cfchnprkizcmfymlezyo`로 얻습니다. 단 **신형 `secret` 키는 CLI가 마스킹해서(`·` 문자로) 내려주므로** 대시보드에서 복사해 `SUPABASE_SECRET_KEY`에 넣거나, 위처럼 legacy `service_role` 키를 쓰세요. 마스킹된 값이 들어오면 503으로 즉시 잡습니다.
- OpenAI 키가 없거나 틀려도 프로그램은 죽지 않습니다. 명세 §13.3에 따라 2단계 AI 검증만 건너뛰고 `정상(AI 미검증)`으로 표시하며 취합은 진행됩니다.
- Supabase 키가 없으면 **인증이 열리는 게 아니라 503으로 닫힙니다**(fail-closed). 인증 없이 돌리려면 `AUTH_DISABLED=1`을 명시해야 하고, 기동 시 경고를 찍습니다.

---

## 1. 실행 방법

```bash
pip install openpyxl pillow openai fastapi uvicorn python-multipart httpx pyjwt cryptography
```

### 웹 (권장)

```bash
python3 -m uvicorn server:app --port 8000
```

`http://localhost:8000/login.html` 로 시작합니다. 로그인하지 않고 다른 화면에 가면 여기로 돌려보냅니다.

| 화면 | 경로 |
|---|---|
| 로그인·회원가입 | `/login.html` |
| AI 양식 생성 · 기존 양식 등록 | `/create.html` |
| 엑셀 취합 (4단계) | `/aggregate.html` |
| 한글(hwpx) 병합 | `/hwpx.html` |
| 작업 이력 | `/history.html` |
| 관리자 콘솔 | `/admin.html` (관리자만) |

`Supabase`를 띄우기 어려운 환경에서 취합만 보려면 `AUTH_DISABLED=1 python3 -m uvicorn server:app --port 8000` 으로 인증을 끕니다(로컬 개발 전용). **F1 양식 생성은 이 모드에서 쓸 수 없습니다** — `user["id"]`가 없어 503입니다. 브라우저로 F1을 볼 때는 인증을 켜세요(`.claude/launch.json`의 `chwihap-auth`가 그 설정입니다).

### CLI

```bash
python3 aggregate.py <입력폴더> --mode B
```

주요 옵션:

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--mode` | `B` | 합성 모드 `A`/`B`/`C`/`D` (§9) |
| `--model` | `gpt-5-mini` | LLM 모델. `.env`의 `OPENAI_MODEL`로도 지정 가능 |
| `--no-ai` | off | 2단계 AI 재검증 생략(규칙검증만) |
| `--out` | `merged.xlsx` | 결과 파일 경로 |
| `--report` | `error_report.xlsx` | 오류 리포트 경로 |
| `--include-anomalous` | off | 오류 등급 파일까지 강제로 취합 포함(§7) |
| `--rules` | 없음 | 작성기준 JSON 경로(§5.4 확장 스키마). 키 컬럼 `"key": true`, 선택 입력 컬럼 `"required": false` |
| `--group-map` | 없음 | 모드 C 그룹 매핑 JSON `{시트명: 그룹명}` |
| `--summary-cols` | 없음 | 모드 D 요약 지표 컬럼(콤마 구분) |
| `--include-hidden` | off | 숨김 시트도 포함 |

### 양식 생성 CLI (F1)

```bash
python3 formgen.py "부서별 예산 집행 현황 양식 만들어줘" -o form.xlsx --rules-out rules.json
```

문답을 터미널에서 주고받고(한 번에 한 주제), 사양이 확정되면 파일을 만듭니다.
`--rules-out`으로 뽑은 작성기준은 **취합의 `--rules`에 그대로** 넣을 수 있습니다.
**실제 OpenAI 호출이 일어납니다**(문답 턴마다 1회 + 저작 1회).

### hwpx 병합 CLI (F3)

```bash
python3 hwpx_merge.py a.hwpx b.hwpx c.hwpx -o merged.hwpx
```

지정한 순서대로 **파일당 별도 구역**으로 이어 붙입니다(각 문서가 자기 용지·여백·머리말을 유지).
새 의존성은 없습니다(표준 라이브러리 `zipfile`+`re`). 실행하면 수량 대조·ID 공간·미대응 참조·
한계를 함께 찍고, **해석 불가 참조가 1건이라도 있으면 결과 파일을 쓰지 않고 실패로 끝냅니다.**

자체 점검:

```bash
python3 test_aggregate.py
python3 test_server.py
python3 test_hwpx.py
python3 test_formgen.py
python3 test_auth.py
```

앞의 네 개는 9개·13개·5개·9개 시나리오가 전부 통과해야 정상이며 **네트워크·API 키 없이** 돌아갑니다(AI 계층은 스텁, 인증은 `AUTH_DISABLED=1`로 끔).

`test_auth.py`는 다릅니다 — **실제 Supabase 프로젝트를 상대로** 14개 시나리오를 돌아 네트워크와 `.env`의 `SUPABASE_*`가 필요합니다. 테스트 계정은 매 실행 만들고 지웁니다(사번 접두사 `zz-test-`).

---

## 2. 구현 범위 — 무엇이 되고 무엇이 안 되는지

### 구현 완료

| 명세 | 구현 내용 |
|---|---|
| §2 업로드·구조 인식 | 시트 열거, 헤더 추정(병합 제목행 skip + 다음 행 타입 대비 검증), 데이터 영역 인식(공백 2행 허용), 숨김 시트 제외. **세로 병합 셀 값 채우기**, **합계·각주 행 제외**, 표가 아닌 시트(작성 안내 등)는 경고로 건너뛰기 |
| §2.1 오류 안내 | 포맷 오류·헤더 미발견·빈 시트·데이터 없음·파일 손상 5종을 사유별 메시지로 구분 |
| §3 유형 분류 | 작성기준 우선 → 키워드(국/영문) 1차 후보 → 값 타입 샘플링 최종 판정. 불확실 시 자유서술형 |
| §4.1 1단계 규칙검증 | **전량 스캔**(첫 위반에서 중단하지 않음), 누락 → 정합성 → 중복/충돌 순서 |
| §4.2 파일 단위 게이팅 | 파일 내 위반 셀이 1개라도 있으면 그 파일은 AI 호출 자체를 하지 않음 |
| §4.2 컨텍스트 규칙 | 코드표·자유서술형은 인접 컬럼 포함, 패턴 기반은 미포함, 정량적은 같은 컬럼 분포 통계 포함 |
| §4.2.1 개별 파싱 실패 | `이상(AI 재검증 실패)` + `[확인필요]` 태그, 내부 등급은 오류 |
| §5 유형별 규칙 | 정성(코드표/패턴/자유서술) · 정량(타입·범위·자릿수) · 이미지(확장자·용량·해상도·해시중복·anchor 범위) |
| §6 이상판별 출력 | 정상/이상 이진 표시 + 사유에 `[경미]`/`[심각]`/`[확인필요]` 태그. 검증 단계는 화면 비표시 |
| §7 대표 등급 | 파일 내 최악 등급 채택(오류 > 경고 > 정상), 오류는 기본 제외 |
| §8 전처리 | 경고 항목 자동교정 + **원본값 보존**(감사 추적). AI 발견 이상은 자동교정 제외 |
| §9 합성 A/B/C/D | 모드 D 요약은 SUMIF 수식으로 생성(하드코딩 아님), 컬럼 부재 시 `N/A` |
| §10 리포트 | 2시트 구성 — `오류 목록`(검증 단계 구분 포함) + `자동교정 이력` |
| §13.2 마스킹 | 주민번호·전화·이메일 자동 치환. **전송 payload 전체**에 적용(인접 컬럼·분포 샘플 포함) |
| §13.3 장애 폴백 | AI 서비스 장애 시 2단계 생략 → `정상(AI 미검증)` |
| 부서명 지정 | 업로드 단계에서 파일별 부서명을 추천 칩으로 고르거나 직접 입력. 같이 올라온 파일명의 공통어를 제목으로 판단해 후보를 정렬한다. 미지정 시 종전대로 파일명 전체 (PRD 미반영 — v2.0 필요) |
| F2-13 키 컬럼 지정 | 업로드 단계에서 작업 단위 전역 키 컬럼 1개 선택(기본 `자동`). 자동 판정은 키워드 → **값이 고유한 첫 컬럼** → 첫 컬럼 순. 작성기준 `{헤더명: {"key": true}}`가 우선 (PRD v1.8 신규) |
| 취합할 표 고르기 | 업로드 단계에서 취합 대상 표를 다중 선택(기본 전체). 실제 양식에는 작성 가이드·비워 둔 대장처럼 대상이 아닌 시트가 섞여 있다. 한 시트에 표가 여러 개면 `시트명 (1)`·`(2)`로 나뉘어 각각 고를 수 있다. 고르지 않은 표는 사유도 남기지 않는다 |
| 한 시트 여러 표 · 계층 헤더 | 빈 행 한 줄만 두고 이어지는 다음 표를 따로 읽고(`_starts_new_table`), 계층 헤더는 병합으로 범위를 잡아 한 줄로 합친다(`_compose_headers`). 실측 근거는 §4 |
| 선택 입력 컬럼 지정 | 업로드 단계에서 비워둬도 되는 컬럼을 다중 선택. 작성기준 `{헤더명: {"required": false}}`로 전달. 미지정 시 종전대로 전 컬럼 필수 (PRD v1.9 F2-14) |
| 화면 2 홈 | 실제 기능 3개(양식 생성·등록 / 엑셀 취합 / 한글 병합)로 카드를 바꾸고 각 화면으로 연결. 사이드바에 한글 병합·작업 이력 추가, 사번 표시·로그아웃 동작. **없는 기능을 광고하던 문구를 걷어냈다** — '공유된 링크로 데이터가 실시간 수집된다'는 PRD §7이 범위에서 제외한 온라인 수집 포털이고, 'B2B 보안(기업용 암호화)'은 근거 없는 문구였다. 죽은 링크(이용약관·개인정보처리방침·고객지원)는 링크가 아니게 뒀다 |
| 총괄표류(피벗형) 취합 | 업로드 단계에서 표를 **피벗형으로 지정**하면 월×지표 표를 한 줄로 눕히고 시트명을 `총괄표(전개)`로 통일한다. 컬럼은 `1월 신고`처럼 *행 라벨 + 원래 컬럼명*이 되고 파일 하나가 한 행이 된다 — 모드 B·C로 한 시트에 모인다. 라벨 컬럼은 이름이 있는 첫 컬럼에서 읽는다(실제 총괄표는 B열부터 시작한다). 합계 행은 종전대로 제외되고, 눕히면서 뺀 사진 수를 밝힌다 |
| 진행률 폴링 | 업로드 파싱·검토·취합이 즉시 `{job_id}`를 돌려주고 `GET /api/job/{jid}`로 단계·진행률을 폴링. 잡 상태는 프로세스 메모리 (PRD v1.9 F2-15) |
| F4-1 사번 로그인 | 사번 → `{사번}@internal.local` 가상 이메일로 Supabase Auth 위임. 사번은 대소문자 무관. 토큰은 httpOnly·SameSite=Lax 쿠키, 검증은 JWKS(ES256) 로컬. 취합 API 전체에 인증 + 세션·잡 소유자 격리(§6.2) |
| 영속화 | 업로드 xlsx는 Storage `uploads`, 결과·리포트는 `results`. 기록은 `projects`·`uploaded_files`·`review_results`·`aggregation_jobs`. 취합 시작 시 프로젝트 자동 생성 |
| F4-2 작업 이력 | [ui/history.html](ui/history.html) — 검색(작업명·파일명)·유형 필터(전체/엑셀/한글), 과거 결과 재다운로드. 세션이 사라진 뒤에도 동작 |
| F4-3 Admin 콘솔 | [ui/admin.html](ui/admin.html) — 지표 카드, 계정 권한·정지·삭제, 보관 기간, 사용 로그(검색). 관리자만 접근하고 본인 계정은 스스로 잠그거나 지울 수 없다 |
| 보관 기간 정리 | Admin에서 **무엇이 지워질지 먼저 세어 보이고**(기준일·작업 수·파일 수·용량) 누르면 지운다. 양식을 담은 프로젝트는 대상에서 빼고 몇 건을 뺐는지 밝힌다 — 양식은 두고두고 다시 쓰는 자산이라 보관 기간의 대상이 아니다. 주기 실행은 `python3 manage_users.py purge-expired`(`--dry-run` 있음)를 cron에 걸면 된다 |
| 업로드 제한 조정 | 파일당·개수·합계 상한을 Admin에서 바꾼다. 값은 정책 한 곳(`retention_policy`)에 있고 **검사·Admin·업로드 화면 문구가 모두 그 값을 본다** — 종전에는 서버 상수 하나와 화면 세 곳의 문구가 따로 적혀 있어 상한을 바꾸면 화면이 거짓말을 하게 돼 있었다 |
| 감사 로그 | 계정 변경·삭제, 정책 변경, 취합 완료를 `audit_logs`에 남긴다. 행위자 계정이 지워지면 `actor_id`만 NULL이 되고 기록은 남는다 |
| F1-1·2 문답 (AI 양식 생성) | [formgen.py](formgen.py) `intake_turn` + [ui/create.html](ui/create.html) 화면 3. 질문 순서·완료 판정은 LLM이 주도하고(명세 D3), 백엔드가 **완결성 루브릭**(양식명·항목·유형·필수여부·날짜/금액 형식)과 **최대 30턴**만 가드로 건다. LLM이 "다 됐다"고 해도 사양에 빈 곳이 있으면 완료로 인정하지 않고 부족 항목을 되돌린다. 생성 버튼은 **서버가 내린 `spec_complete`로만** 켜진다 |
| F1-4 양식 파일 생성 | LLM이 셀 단위로 `workbook_json`을 저작하고(명세 D1) `materialize`가 해석 없이 xlsx로 옮긴다. **산출물은 신뢰하지 않는다** — 수식 함수 화이트리스트·외부 참조 금지·셀 수 상한·시트명 규칙·스타일 속성 화이트리스트를 통과한 것만 파일이 된다(실패 시 위반 목록을 알려 최대 2회 재저작). 결과는 화면에서 표로 미리보기 |
| F1-5·6 작성기준 → 취합 연결 | `spec_json.fields`에서 `field_rules`를 기계적으로 파생하고(LLM 재호출 없음), 동시에 **취합 엔진이 이미 먹는 형태**(`{헤더명:{"required":bool}}`)를 내놓는다. 생성한 양식으로 회신받은 파일에서 선택 항목이 오탐되지 않는다. **키 컬럼은 넣지 않는다** — 사양에 대응 개념이 없어 첫 항목이나 `dept_code`를 키로 단정하면 없는 근거로 값을 만드는 셈이다 |
| 생성 → 취합 화면 연결 | 취합 업로드 단계에서 **생성해 둔 양식을 골라 작성기준을 가져온다**([ui/aggregate.html](ui/aggregate.html) `#template-pick`). 서버는 손대지 않았다 — 내려오는 형태가 검토 요청의 `optional_cols`와 같은 단위라 **기존 '선택 입력 컬럼' 칩에 채운다.** 무엇이 적용됐는지 사용자가 보고 고칠 수 있고, 맞지 않은 항목은 몇 개인지·무엇인지 밝힌다(회신본이 다른 양식이면 여기서 드러난다) |
| AI 전송 동의 | 명세 §9.1. 입력이 외부 API로 나가므로 동의 전에는 F1 API 전체가 403(`AI_CONSENT_REQUIRED`)이고 화면은 모달을 띄운다. `profiles.ai_consent_at`에 1회 기록. **취합(F2)의 2단계 재검증은 이 동의와 별개**(기존 동작 유지) |
| F1-7 대화형 수정 | 결과 카드에서 "비고 빼고 교육시간 추가해줘"로 고치면 **사양을 고쳐 다시 저작**하고 버전을 올린다(`POST /api/form/{sid}/revise`). 새 프롬프트를 만들지 않았다 — 수정 요청은 결국 문답 한 턴이고, 고쳐진 사양으로 저작·검증·변환을 다시 태우면 사양과 파일이 어긋날 수 없다. 무엇을 고칠지 확정되지 않으면 **파일을 건드리지 않고 되묻는다.** 이전 버전 파일은 버전별 경로로 남는다. **등록 양식(F6)은 400으로 막는다**(인지 E3) |
| F6-1~6 기존 양식 인지·등록 | 화면 3에 **파일 첨부**가 붙었다. xlsx를 붙이면 구조를 읽어 등록 후보로 삼고, 첫 턴에서 AI가 의도를 판정해(`recognize`/`generate`/`ambiguous`) 인지 모드로 분기한다. 모호하면 되묻고 화면에 분기 버튼 2개를 띄운다. 확정하면 **원본 파일을 고치지 않고 그대로** 양식으로 등록하고(바이트 무변경), 작성기준을 파생해 취합에 연결한다. 등록은 동기다(잡·폴링 없음) |
| F6-7 작성기준 보정 | 등록 양식은 원본을 못 고치는 대신(E3) **작성기준을 결과 화면에서 고친다** — 항목 칩을 눌러 필수·선택을 바꾸고 저장하면 `PATCH /api/form/templates/{id}/rules`가 사양과 `field_rules`를 함께 갈아끼운다. 원본 파일·버전은 그대로다. 받는 모양은 `GET .../rules`와 같아 취합 화면이 이미 먹는 계약을 그대로 쓴다 |
| F6 등록 대상 지정 | xlsx를 둘 이상 붙이면 첨부 목록에서 등록 대상을 골라 바꾼다(인지 §5.2). 첨부를 지우는 기능이 없어 순서를 잘못 올리면 문답을 새로 시작해야 했다 |
| F1-3 첨부 컨텍스트 | 같은 첨부 경로를 쓴다. 의도가 `generate`면 등록 대상 표시를 내려 참고 자료로 전환하고 문답이 그 내용을 보고 진행한다. hwpx는 본문 텍스트만 뽑아 참고로만 쓰고(등록 대상 불가), png는 받지 않는다 |
| 문답 재진입 | `create.html`이 `GET /api/form/{sid}`를 쓴다. `?sid=`로 열거나 새로고침하면 대화·사양·모드·첨부가 복원된다(끝난 문답은 되살리지 않는다). 종전에는 확정된 사양을 두고 문답을 처음부터 다시 해야 했다 |
| F3-1·2·4·5·6 hwpx 병합(화면·API) | [ui/hwpx.html](ui/hwpx.html) 화면 8 — 다중 업로드(드래그 앤 드롭 포함), **행을 끌어 순서 변경**(브라우저 기본 DnD, 라이브러리 없음), 체크 해제로 제외, 단계·진행률 폴링, 결과 지표·한계 안내·다운로드. 업로드 상한은 엑셀 취합과 같은 상수를 공유하고, 만료 세션의 임시 폴더는 새 세션이 생길 때 정리된다(F3-5) |
| F3-3 hwpx 병합(엔진) | [hwpx_merge.py](hwpx_merge.py) — OWPML 직접 병합. 파일당 별도 구역으로 이어 붙이고, 문서마다 다른 ID 공간(style·charPr·paraPr·borderFill·tabPr·numbering·언어별 font)을 매핑으로 재번호. BinData 이름 충돌 회피, `content.hpf` manifest·spine 재생성, `hh:head secCnt` 갱신. 검증 미통과 시 파일 미작성 |

### 미구현 (의도적 제외)

명세는 React + FastAPI + Supabase 풀스택이지만, 지금은 **CLI + FastAPI + 정적 HTML**입니다. React 빌드 체인과 Supabase를 빼고 취합 코어를 먼저 실제로 동작시키는 쪽을 택했습니다.

| 미구현 | 사유 / 다음 단계 |
|---|---|
| 작업 재개 | 재시작 후 **기록과 산출물은 남지만** 중단된 취합을 그 자리에서 이어서 하지는 못한다. 파싱된 작업 집합이 메모리에 있어 되살리려면 파일을 다시 내려받아 다시 읽어야 한다 |
| 진행 중 잡 상태 | `aggregation_jobs`에 남기지만 폴링용 상태는 프로세스 메모리라, 재시작하면 진행률 추적이 끊긴다(기록은 남음) |
| F6-7 재인지(새 버전 등록) | 등록 양식의 작성기준을 고치는 것은 취합 화면의 선택 입력 칩으로 되지만, `PATCH /form-templates/{id}/field-rules`와 `version+1` 재등록은 없다. 양식이 바뀌면 새로 등록하면 되므로 뒤로 미뤘다 |
| 이미지 AI Vision | 명세 §14에서 v2 백로그로 지정된 항목 |
| 파일 간 교차 중복 | 명세 §14 v2 백로그. 중복 판정은 파일 내부로 한정 |

---

## 3. 코드 구조

| 파일 | 역할 |
|---|---|
| [aggregate.py](aggregate.py) | 취합 엔진 + CLI. 아래 표 참조 |
| [server.py](server.py) | FastAPI. 세션·잡 모두 프로세스 메모리 `dict` + 임시 디렉터리라 **서버 재시작 시 소실**. 긴 작업은 `_start_job`으로 스레드에 넘기고 `GET /api/job/{jid}` 폴링. `/api` 라우트 뒤에 `ui/`를 정적 마운트 |
| [ui/aggregate.html](ui/aggregate.html) | 취합 4단계 단일 페이지. Tailwind CDN + 바닐라 JS, 빌드 없음. 토큰·셸은 `index.html`에서 그대로 이식 |
| [auth.py](auth.py) | F4-1 인증. 가상 이메일 치환·가입·로그인·JWT 검증. 가상 이메일은 이 모듈 밖으로 안 나간다 |
| [manage_users.py](manage_users.py) | 계정 CLI(`list`/`promote`/`demote`/`suspend`/`activate`). Admin 화면이 생겼으니 첫 관리자 지정·복구용으로만 남긴다 |
| [store.py](store.py) | Supabase 영속화. Storage 업로드·다운로드, 기록 insert/patch, 이력 조회 |
| [ui/login.html](ui/login.html) | 로그인·회원가입 (design.md 화면 1). aggregate.html과 토큰·테마 셸 공유 |
| [ui/hwpx.html](ui/hwpx.html) | 한글 병합 (design.md 화면 8). 업로드·드래그 순서 지정·진행률·결과. 셸은 history.html과 동일 |
| [ui/history.html](ui/history.html) | 작업 이력 (design.md 화면 9). 검색·유형 필터·재다운로드 |
| [ui/admin.html](ui/admin.html) | Admin 콘솔 (design.md 화면 10). 지표·계정·정책·사용 로그 |
| [test_auth.py](test_auth.py) | 인증·영속화·이력·Admin·F1(수정 포함)·F6 영속화 14개 시나리오. **실제 Supabase를 상대로 돌고 네트워크가 필요하다.** F1 시나리오는 LLM만 스텁으로 바꿔 store는 실제로 탄다 — 스키마 제약을 지나가야 잡히는 결함이 있다(§4) |
| [test_server.py](test_server.py) | API 13개 시나리오(엑셀 8개 + 한글 병합 3개 + 양식 생성 사전조건 1개 + 시계 오차 허용 1개) |
| [formgen.py](formgen.py) | F1 양식 생성 + **F6 기존 양식 인지** 엔진 + CLI. 문답 턴·저작·검증 관문·xlsx 변환·작성기준 파생, 그리고 구조 추출(`read_structure`)·인지 턴(`recognize_turn`)·인지 루브릭(`recognize_gaps`). 구조 판정은 `aggregate`의 헤더·표 인식을 그대로 재사용한다 — 두 번째 판정기를 만들면 취합이 보는 헤더와 인지가 보는 헤더가 갈라진다. 마스킹은 `aggregate.mask`를 그대로 쓴다 |
| [test_formgen.py](test_formgen.py) | 양식 생성·인지 9개 시나리오. **LLM을 스텁으로 바꿔 네트워크·비용 없이** 돈다 |
| [ui/create.html](ui/create.html) | AI 양식 생성 + **기존 양식 등록** (design.md 화면 3 · 인지 §8). 좌측 문답(첨부·분기 버튼) / 우측 사양 카드(신뢰도 배지) / 결과 미리보기 + 동의 모달. 모드에 따라 버튼 하나가 '양식 생성'과 '이대로 등록하기'로 바뀐다 |
| [hwpx_merge.py](hwpx_merge.py) | F3 hwpx 병합 엔진 + CLI. `aggregate.py`와 독립(형식·문제 영역이 다르다). 표준 라이브러리만 씀 |
| [bench_stage2.py](bench_stage2.py) | 2단계 AI 병렬화 실측(순차 대비 배수). **실제 키로 유료 호출이 일어난다** — `--dry`는 호출을 스텁으로 바꿔 호출 수만 센다. 수치는 §4 |
| [test_hwpx.py](test_hwpx.py) | hwpx 병합 5개 시나리오(ID 공간 병합 / 입력 거부 / 검증 실패 시 미작성 / 여러 구역·머리말·15개 규모 / 옮길 수 없는 id 공간). 실제 파일 구조를 재현한 fixture라 **네트워크 없이** 돈다 |

`server.py`는 `aggregate.py`를 import만 하고 수정하지 않습니다. `main()`과 동일한 순서(`read_file` → `review_stage1` → 게이팅 → `review_stage2_many` → `preprocess` → `synthesize` → `write_report`)를 호출하므로 CLI와 웹이 같은 코드 경로를 탑니다.

### aggregate.py 내부

| 영역 | 역할 |
|---|---|
| `load_env` | `.env` → 환경변수 (표준 라이브러리만, python-dotenv 미사용) |
| `Issue` / `Fix` / `SheetData` / `UploadedFile` | 판정 결과 데이터 구조. 명세 §11의 `ReviewResult`/`ErrorItem`에 대응 |
| `read_file` · `_find_header` · `_read_images` | §2 구조 인식 (헤더·데이터 영역·이미지 anchor) |
| `classify_columns` | §3 유형 분류 |
| `review_stage1` · `_validate_text` · `_validate_number` · `_review_duplicates` · `_review_images` | §4.1·§5 유형별 1단계 규칙 |
| `mask` | §13.2 마스킹. **외부 전송 직전 공통 통과 지점** |
| `_ai_items` · `_ai_call` · `review_stage2_many` · `review_stage2` | §4.2 2단계 배치 재검증 + §4.2.1 부분 실패 격리. `_many`가 (파일, 배치)를 평평한 목록으로 만들어 단일 동시성 상한으로 병렬 호출한다 |
| `preprocess` | §8 경고 항목 자동교정 + 원본값 보존 |
| `synthesize` · `_add_summary_sheet` · `_shift_anchor` | §9 합성 모드 A/B/C/D + 이미지 anchor 재배치 |
| `write_report` | §10 리포트 2시트 |
| `main` | CLI 조립 |

단일 파일인 이유: 취합 흐름이 선형이라 모듈 분리 이득이 없었습니다.

---

## 4. 검증 근거

- `python3 test_aggregate.py` — 9개 시나리오 통과 (구조인식·전량스캔 / AI 폴백 / 마스킹 / 합성 4모드 / 이미지 anchor 재배치 / **실제 업무 양식 구조 2계열** / 2단계 병렬·실패격리 / CLI end-to-end)
- 실제 OpenAI 호출 검증(`gpt-5-mini`): 1단계를 전부 통과하지만 내용이 문맥상 틀린 샘플로 확인
  - 무관한 서술(`"오늘 점심은 김치찌개가…"` in 사업내용) → 부적합 판정
  - 이상치 금액(980,000,000 vs 다른 행 수백만원) → 부적합 판정
  - 인접 컬럼 교차 검증: 사업내용↔예산액 불일치를 양방향으로 지적 → §4.2 인접 컨텍스트 동작 확인
  - 세 건 모두 경고 등급이라 취합은 차단되지 않음(2/2건 포함) → §6.2 오탐 방어 동작 확인

- `python3 test_server.py` — 7개 시나리오 통과
- **브라우저 실측**(uvicorn + 실제 xlsx 3개, API 키 없이): 업로드 시 시트·헤더행·행수 인식 → 검토에서 정상 2 / 이상 1, 오류 파일만 기본 해제(선택 2/3건) → 모드 D 취합 → `종합요약` 시트가 `=SUMIF('예산'!A2:A4,$A2,'예산'!E2:E4)` 수식으로 생성됨(§9 하드코딩 금지 충족) → 결과·리포트 다운로드 200
- 오류 파일까지 강제 체크(3/3건) → §10.1 **특이사항** 안내가 셀 위치까지 표시됨
- 라이트/다크 양쪽 렌더 확인, 콘솔 에러 0건
- **키 컬럼(F2-13)**: 첫 컬럼이 부서명처럼 반복되는 xlsx로 `자동` 검토 → '이상'(키 충돌 오탐), 같은 파일에 고유 키 컬럼 지정 → '정상'. 화면에서 전환 확인
- **2단계 AI 실측(v1.9 회차, `gpt-5-mini`)**: 1단계를 통과하는 4행 파일 1개에 **16.0초**. 무관한 서술·인접 컬럼 불일치·이상치 금액을 모두 경미 등급으로 지적해 취합은 차단되지 않음
- **모드 C**: 4개 시트(1~4월)×2파일을 `1·2월→상반기 / 3월→하반기 / 4월 미매핑`으로 취합 → 상반기 9행·하반기 5행·미분류 5행, `구분` 컬럼에 원본 시트명 보존, 미매핑 안내 표시. API·UI 양쪽 확인

### 실제 업무 양식 실측 ① 지사별 자체점검 (v1.9 회차)

한국지역난방공사 지사별 자체점검 리스트 8개(같은 양식의 지사별 회신본)로 확인했습니다. `sampledata/`에 두었고 `.gitignore`로 커밋되지 않습니다.

**종전 엔진은 8개 파일 전부를 '오류'로 판정해 아무것도 취합되지 않았습니다.** 원인 3건을 고쳐 6/8건이 기본 취합 대상이 됐습니다.

| 실제 구조 | 종전 판정 | 원인 |
|---|---|---|
| 첫 시트가 `작성 주의사항` 안내문 | 헤더 미발견 **오류** → 전 파일 제외 | 표가 아닌 시트를 데이터 시트로 취급 |
| `지사`·`개소`가 세로 병합(A5:A17 등) | 필수값 누락 **오류** (분당 29건) | 병합 칸은 파일에 첫 칸만 값이 있음 |
| 표 끝의 `합 계` 행과 `*` 각주 행 | 필수값 누락 **오류** | 레코드로 취급. 게다가 모드 B/D에서 합계가 이중 계상됨 |
| 첫 컬럼 `지사`가 행마다 반복 | 키 충돌 **오류** | 자동 키 판정이 첫 컬럼을 그대로 채택 |

고친 뒤 남은 '오류' 2건은 **정탐**입니다 — 중앙지사가 `지사`·`개소`를 안 채웠고, 분당사업소 28행의 `지사`가 비어 있습니다. 해당 지사가 고쳐야 할 값입니다.

취합 결과도 확인했습니다: 6개 파일 10행, 세로 병합 값이 전 행에 채워지고, 합계 행은 섞이지 않았습니다.

**`부서` 컬럼도 해소했습니다.** 엔진은 부서명을 파일명에서 가져오는데(§9) 실제 파일명은 부서명 위치가 제각각이라(`(수원사업소)2025년도…`, `…리스트(대구)`, `…리스트(양식)_판교지사`) 한 규칙으로 못 잡습니다. 후보를 추천하고 최종 선택은 사용자에게 맡기는 쪽으로 바꿨습니다 — 업로드 화면에 파일별 부서명 입력칸과 추천 칩이 붙습니다.

추천이 정확한 이유는 **같이 올라온 파일명을 함께 보기** 때문입니다. 취합은 같은 양식의 회신본이라 모든 파일에 공통으로 든 말은 제목이고 부서명은 파일마다 다른 부분입니다 — 이게 조직명 꼬리(지사·사업소)보다 강한 신호입니다. `지열지점`은 `지점`으로 끝나지만 8개 파일 전부에 있으니 제목으로 밀려납니다. 실제 8개 파일 모두 정확한 부서명이 1순위로 올라왔습니다.

### 실제 업무 양식 실측 ② 분기별 실적 취합 (5차 회차)

사용자가 **다른 계열** 회신본 12개를 줬습니다(`sampledata/quarterly/`, gitignore됨).
종전 엔진은 **12개 파일 전부를 '오류'로 판정**했고 이슈가 **6,884건**이었습니다. 데이터가
나쁜 게 아니라 구조를 잘못 읽는 것이었습니다.

**이 계열의 구조** — 시트 5개가 성격이 전부 다릅니다.

| 시트 | 성격 |
|---|---|
| `OO지사 총괄표` | 월×지표 피벗. **시트명이 파일마다 다릅니다**(모드 B가 12개로 갈라짐) |
| `1. 아차사고 신고 월별 세부내용` | **표 2개**: r4–33 재해유형×월(3단 헤더·104열) + r35~ 아차사고 상세 목록 |
| `2. 작업중지 요청제 기록 대장` | 대개 비워 둔 표 |
| `3. 근로자 의견수렴, 개선` | 지사별 행 목록 (취합 대상) |
| `<참고> 양식 작성 가이드` | 안내. 12개 파일 모두 동일하고 예시 스크린샷 46장 |

표는 **B열부터** 시작하고 사진이 파일당 50~86장 얹혀 있습니다.

| 증상 | 원인 |
|---|---|
| 둘째 표의 헤더·데이터가 첫 표 행으로 섞임(그 시트 이슈 132건 중 132건) | 빈 행 **한 줄**만 두고 다음 표가 시작되면 `BLANK_ROW_TOLERANCE`로 끊기지 않는다 → `_starts_new_table`이 타입 프로파일이 깨지는 전텍스트 행을 다음 표 헤더로 보고 나눠 읽는다. 이름은 `시트명 (1)`·`(2)` |
| `중대성`이 조치 전·후 두 컬럼에 같은 이름으로 붙음 | `_find_header`가 계층 헤더의 **맨 아래 단**을 집는다 → 병합으로 헤더 범위를 잡아 한 줄로 합친다(`조치 전 위험성평가 중대성`·`1월 내부 신고`). 상위 이름은 자기 값이 있는 칸에만 붙여 중복을 만들지 않는다 |
| B열부터 시작하는 표의 `합계` 행이 레코드로 들어감 | `_is_summary_or_note`가 `row[0]`만 봐서 A열이 비면 표기를 놓친다. 모드 B/D에서 이중 계상되는, 첫 계열에서 고쳤던 것과 같은 종류 |
| 순번만 1..N으로 매겨 둔 빈 서식 행이 데이터로(25행 중 15행) | 양식이 순번을 미리 적어 두고 회신자가 일부만 채운다. 첫 컬럼에 숫자 하나만 있는 행은 제외하고 몇 행을 뺐는지 밝힌다 |
| 사진 열이 둘인 양식에서 정상 입력 370건이 오류 | 명세 §5.3은 "동일 키에 대해 **필드당**"인데 **행 단위**로 셌다. 한 칸 기준으로 바꿨다(370 → 12건, 남은 12건은 실제로 한 칸에 2장) |

**취합할 표 고르기(신규)** — 시트 5개 중 취합 대상은 1~2개뿐이라 '전 시트 취합'이 의미가
없습니다. 어느 시트가 데이터인지는 담당자가 아니까 이름 규칙으로 맞히지 않고 골라 받습니다
(`read_file(include_sheets=...)` + 업로드 화면의 표 선택). 이것만으로 작성 가이드 564건 +
빈 대장 132건이 사라집니다. 선택이 바뀌면 원본에서 다시 읽습니다 — 뺐던 표를 되살릴 근거가
파일뿐입니다.

**결과**: 의견수렴 **97행** / 아차사고 상세 **71행**, 12개 지사가 각각 한 시트로 합쳐지고
헤더 불일치 분리 0건. 이슈 6,884 → **571건**이고 남은 것은 대체로 정탐입니다 — 삼송지사가
의견수렴 25행을 적고 사진은 6장만 붙였고, 조치 전인 건들의 조치 관련 칸이 비어 있습니다
(그 컬럼을 선택 입력으로 지정하면 391건, 대부분 사진이 셀 경계에 걸친 경고).

**알아둘 경계**: 이미지 컬럼은 `review_stage1`이 통째로 건너뛰므로 **사진 미첨부는 잡히지
않습니다**. 사진이 한 장도 없는 컬럼만 텍스트 컬럼으로 분류돼 누락으로 잡히는 비대칭이
있습니다(삼송지사가 그렇게 걸렸습니다). 명세 §5.3의 이미지 규칙에 '미첨부'가 없어 그대로
뒀습니다 — 사진 필수 여부는 양식 주인이 작성기준으로 정할 일입니다.

### 이번 회차에 해소한 미검증 항목

**이미지 anchor 재배치(§9)** — 실측 결과 3건 모두 깨져 있어 수정했습니다([aggregate.py](aggregate.py) `_shift_anchor`).

| 증상 | 원인 |
|---|---|
| 모드 A에서 사진이 2행 아래 엉뚱한 데이터 행에 붙음 | 출력은 헤더를 1행으로 당겨 쓰는데 anchor를 안 옮겼음(주석은 "이동 없음"이라 단정) |
| 모드 B/C/D에서 증빙사진이 예산액 칸 위에 얹힘 | 앞에 끼워 넣는 `구분`·`부서`만큼 열이 밀리는데 열 오프셋 미적용 |
| 셀에 맞춰 줄여 놓은 사진이 원본 픽셀 크기로 되살아남 | 표시 크기는 anchor의 `ext`에 있는데 openpyxl의 `im.width/height`는 원본 픽셀을 돌려줌 |

두 셀 앵커는 `OneCellAnchor`로 뭉개지 않고 그대로 재구성합니다(점유 범위·셀내 EMU 오프셋·`editAs` 보존). 실측: 원본 `D4:F6` → 모드 A `D2:F4`, 모드 B `E2:G4`. `test_aggregate.py`에 시나리오 추가.

**대량 파일 성능** — 30개 파일 / 총 99,990행 / 3.4MB, AI 계층 제외, M-series macOS 기준:

| 단계 | 소요 |
|---|---|
| `read_file`(구조 인식) | 3.48s (52%) |
| `save`(결과 쓰기) | 2.02s (30%) |
| `synthesize`(모드 B) | 0.93s (14%) |
| `review_stage1` | 0.16s |
| `preprocess` | 0.11s |
| **합계** | **6.71s** (최대 RSS 378MB, 오탐 0건) |

82%가 openpyxl의 xlsx 파싱·직렬화라 미세 최적화 여지가 없습니다.

웹 경로 실측(업로드→검토→취합)은 **6.9초**입니다. 검토 단계가 0.0초인 것은 종전에 업로드에서 한 번, 검토에서 또 한 번 하던 재파싱을 없앴기 때문입니다(취합이 돌아 `preprocess`가 셀을 고친 뒤에만 원본을 다시 읽습니다).

> **측정 주의** — 이 표의 초기값(합계 36.3초)은 `tracemalloc`을 켠 채 잰 값이라 5배 넘게 부풀려져 있었습니다. 파이썬에서 openpyxl 같은 할당 집약적 코드를 잴 때 `tracemalloc`은 측정 자체를 크게 왜곡합니다. 메모리는 `resource.getrusage`의 RSS로 따로 재세요.

**2단계 AI가 실제 지배적 비용** — 실제 키(`gpt-5-mini`)로 4행짜리 파일 1개를 재검증하는 데 **16.0초**가 걸렸습니다. 1단계 규칙검증 전체(0.16초)의 100배입니다. 즉 **진행률 표시가 필요한 이유는 엑셀 처리(7초)가 아니라 AI 재검증**입니다.

그래서 병렬화했습니다([aggregate.py](aggregate.py) `review_stage2_many`). (파일, 배치)를 하나의 평평한 작업 목록으로 만들어 **단일 동시성 상한**으로 돌립니다 — 파일별로 스레드 풀을 중첩하면 동시 호출 수를 통제할 수 없습니다. 동시 호출 수는 `AI_CONCURRENCY`(기본 6)로 조절합니다.

실패 격리는 파일 단위로 유지합니다(§13.3) — 어느 배치가 터지면 그 파일만 `정상(AI 미검증)`이 되고 다른 파일 판정은 살아 있습니다.

**미측정**: 실제 키로 돌린 순차 대비 병렬 배수. 회귀 테스트는 `_ai_call`을 지연·실패 스텁으로 바꿔 호출 스케줄만 결정적으로 검증합니다(네트워크·비용 없음). 실제 배수는 업무 파일로 한 번 재보는 편이 정확합니다 — 모델 응답 시간과 계정 rate limit에 달려 있습니다.

**미검증(남음)**: 지사별 자체점검 리스트 외 다른 양식에서의 구조 인식, 30개 규모의 2단계 전체 소요(1개 16초 기준 추정치만 있음), 실제 키로 돌린 병렬 배수.

### AI 양식 생성 실측 (F1)

**사용자가 개발 중 실제 OpenAI 호출을 승인했습니다**(2026-08-04). 스텁으로는 프롬프트 품질과
산출물 레이아웃을 확인할 수 없어서, `gpt-5-mini`로 전 구간을 돌렸습니다.

한 바퀴(문답 5턴 + 저작 1회 = **6호출 · 16k 토큰 · 약 $0.022**): "지사별 안전점검 결과 취합
양식 만들어줘" → 문답으로 항목 6개·형식·필수여부 확정 → 양식 생성 → 지사 2곳이 값을 채운
회신본 → 취합 3행. **오탐 0건**입니다. 나온 양식은 제목 병합·작성요령 행·필수 항목 `*` 표시·
고정창·열너비·날짜/목록 유효성 검사를 갖춘 실무 형태였습니다.

**실제 호출이 잡은 결함 3건** — 스텁으로는 드러나지 않는 종류입니다.

| 증상 | 원인 |
|---|---|
| 저작이 3회 전부 검증에 막혀 사용자에게 아무 파일도 안 나감 | 모델이 드롭다운 목록을 `source`가 아니라 **`values`**로, 정수 검사를 `whole`이 아니라 **`integer`**로 내보냈다. 회차마다 흔들려 프롬프트만으로는 안 막힌다 → 이름만 다른 같은 제약이라 `normalize_workbook`에서 표준 키로 옮긴다 |
| 코드표가 목록으로 주어지지 않았는데 `["전체 지사"]`라는 문구로 드롭다운을 만듦 | 회신자가 값을 넣을 수 없는 양식이 된다. **실제 목록이 있을 때만** 드롭다운을 만들도록 프롬프트에 못 박았다 |
| "비고만 선택이고 나머지는 필수"라고 명시했는데 `조치기한`이 선택으로 남음 | 모델이 앞서 자기가 제안한 값을 유지했다. 필수/선택이 틀리면 취합에서 그대로 오판이 된다 → **사용자 정정이 이긴다**는 지시를 넣어 해소 |

**구조 결함 3건**도 같이 잡았습니다.

| 증상 | 원인 |
|---|---|
| 숫자 유효성 검사가 `operator`·수식 없이 나가 엑셀이 손상으로 볼 수 있는 파일 | materializer의 기본 분기. 상·하한을 지어내지 않고(사용자가 요구하지 않은 제약이 양식에 박힌다) 안내 문구만 남기고 **몇 건을 낮춰 처리했는지 밝힌다** |
| `/api/form/templates` 요청이 404 | `/api/form/{sid}`를 먼저 선언해 FastAPI가 `sid="templates"`로 삼켰다. 리터럴 경로를 앞으로 옮겼다 |
| Supabase를 못 쓸 때 없는 문답 조회가 404 대신 500 | REST 오류가 그대로 새어나갔다. 세션 접근이 모두 지나는 `_my_session`에서 한 번 막는다 |

**미검증(남음)**: 30턴 가드 실동작(스텁 테스트는 있고 실제 키로는 안 돌려봤습니다).

### F1 실동작 확인 (5차 회차) — 브라우저에서 눌러보고 결함 3건

마이그레이션 반영 뒤 `/create.html`을 실제로 눌러 문답 → 생성 → 다운로드를 돌렸습니다.
**§0.1의 마이그레이션은 이미 원격에 반영돼 있었고**(4차 기록이 낡았습니다) 빠진 것은 적용
기록뿐이라 `migration repair`로 맞췄습니다.

| 증상 | 원인 |
|---|---|
| '양식 생성'을 누르면 500. 양식 종류와 무관하게 전부 | `field_rules.rule_type` check가 `date/name/amount/dept_code` **4종만** 허용(베이스라인이 design.md §3 초기 스케치를 옮긴 값). 생성기능 기술명세 §3.4와 `formgen.FIELD_TYPES`는 `text`·`number`를 더한 6종이고, 실무 양식에서 가장 흔한 유형이 그 둘이다. `test_formgen.py`는 store를 타지 않아 못 잡는다 → 제약을 명세에 맞춰 넓혔다([20260804043000](supabase/migrations/20260804043000_field_rules_types.sql)). 값을 4종으로 뭉개는 쪽은 택하지 않았다 — 부서명·사번·과정명·비고를 전부 `name`으로 적으면 근거 없는 유형을 기록하게 된다 |
| 계정을 지워도 생성한 양식이 Storage에 남는다(실제로 6개 쌓여 있었다) | `put_form`만 `forms/{project_id}/…`에 쓰는데 정리 코드는 `{project_id}/`를 최상위로 가정하고, 목록 API는 한 단계 아래 폴더를 객체로 돌려주지 않는다. 정리 3곳을 고치는 대신 경로를 다른 두 용도와 같은 규칙(**프로젝트 폴더 바로 아래 평면**)으로 맞췄다 |
| `intake_sessions.turn_count`가 실제보다 1 많다(6턴 문답이 7로 기록) | 개수를 담는 컬럼에 다음 턴 번호를 넣고 있었다. 가드는 별도 값으로 판정해 동작에는 영향이 없었다 |

**실측**(gpt-5-mini): 사양을 한 번에 적으면 1턴에 확정됩니다 — 문답 1 + 저작 1 =
**2호출 · 5,215토큰**(in 1,579 / out 3,636). 6턴으로 주고받은 회차는 계측을 붙이기 전이라
수치가 없습니다. 나온 양식은 제목·작성안내 병합, 헤더 `*` 표시, 고정창 `A4`, 항목별 열너비,
날짜 유효성검사를 갖춘 실무 형태였고, **코드표를 주지 않은 `부서명`에 드롭다운을 만들지
않았습니다.** 숫자 열은 범위가 없어 안내만 남기고 몇 건인지 화면에 밝혔습니다.
4차에 틀렸던 두 지점("비고만 선택", 없는 목록으로 드롭다운)이 이번엔 맞았습니다.

`test_auth.py`에 F1 영속화 시나리오를 넣었습니다(13개) — **LLM만 스텁이고 store는 실제
Supabase를 탑니다.** 그게 위 두 결함을 잡은 차이입니다. `rule_type` 집합을
`formgen.FIELD_TYPES`와 직접 대조하고, 양식 파일이 정리되는 자리에 있는지도 확인합니다.
유료 호출 사용량 계측이 아예 없어 [formgen.py](formgen.py) 호출 지점에 토큰 로깅을 붙였습니다.

### 생성 → 취합 화면 연결 (5차 회차)

브라우저 실측: 같은 파일이 **양식 선택 전 이슈 3건 → 후 2건**(`보수여부`가 선택 입력으로
바뀜) → 모드 B 취합 2파일·1시트. 작성기준이 실제 판정에 반영되는 것을 화면에서 확인했습니다.
안내는 "양식 항목 9개 중 8개가 맞았고 선택 입력 2개를 채웠습니다. 파일에 없는 항목 1개:
담당자 서명"처럼 미대응을 밝힙니다.

**브라우저 확인이 잡은 결함**: **읽지 못한 파일이 검토에서 '정상'으로 표시됐습니다.**
재검토 초기화가 `read_file`이 남긴 실패 사유까지 지우고, 1단계는 읽을 수 없는 파일을
건너뛰므로 이슈 0건 → `status`가 '정상'이 됩니다(첫 검토도 같은 경로). 실제 업무 파일 8개는
전부 읽혀서 종전 회차에 드러나지 않았습니다. 취합 대상에서는 `selectable=False`로 빠지고
있었으니 피해는 표기에 한정되지만, 읽지도 못한 파일을 '정상'이라 적는 것은 이 프로젝트가
피해온 종류의 값입니다. `test_server.py`에 시나리오를 넣어 고정했습니다(12개).

양식 선택 UI 자체는 순수 프론트라 서버 테스트로 덮을 수 없습니다. 이 UI가 소비하는 API
계약(목록·rules shape)은 `test_auth.py`의 F1 시나리오가 잠급니다.

**GUI 확인 메모**: Browser 패널의 뷰포트가 0×0으로 접혀 스크린샷이 조각나게 나오는 일이
있었습니다. `resize_window`로 크기를 다시 잡으면 정상으로 돌아오고, 렌더 확인은 계산된
스타일(`getComputedStyle`)로 하는 편이 확실합니다. 이 패널에는 파일 업로드 도구가 없어
`fetch` → `File` → `DataTransfer` → `change` 이벤트로 실제 업로드 핸들러를 태웠습니다
(base64를 인라인으로 밀어넣으면 바이트가 깨집니다 — 손상 파일 안내가 정확히 떠서 알았습니다).

### 2단계 AI 병렬화 실측 (6차 회차) — 사용자 승인 후 실제 키로

**순차 677.5초 → 병렬(동시성 6) 112.0초 = 6.05배.** 동시성에 거의 선형이라 이 규모에서는
rate limit이 아니라 **응답 지연이 전부**입니다. 30개 파일·1,200항목·배치(=호출) 30건을 두 번
돌렸고(총 60호출), 30/30 파일이 판정 완료·AI 미검증 0건·오탐 0건이었습니다.
**파일당 순차 22.6초**입니다(4행 파일 1개 16초였던 종전 실측보다 항목이 많습니다 — 40항목/파일).

**토큰 382,190**(in 211,140 / out 171,050 · 호출당 평균 6,369). gpt-5-mini 기준 대략 $0.4입니다.
이 수치를 잴 수 있게 된 것은 같은 회차에 `_ai_call`에 계측을 붙였기 때문입니다.

판단 근거로 쓸 것: **30개 파일 규모의 2단계는 기본 설정(6)으로 약 2분**입니다. 더 줄이려면
`AI_CONCURRENCY`를 올리면 되고, 이 계정·모델에서는 6까지 감속이 없었습니다. 올릴 때 rate limit에
걸리면 `정상(AI 미검증)`으로 조용히 넘어가는 것이 아니라 파일 단위로 그렇게 표시됩니다(§13.3).

재현: `python3 bench_stage2.py --files 30 --workers 6`(`--dry`로 호출 수만 셀 수 있습니다).

### 총괄표류(피벗형) 취합 (6차 회차)

**엑셀에서 마지막으로 남아 있던 구조입니다.** 총괄표는 두 가지 이유로 종전 방식이 성립하지
않았습니다 — ① 시트명이 파일마다 달라(`강남지사 총괄표`) 모드 B가 파일 수만큼 시트를 갈라 놓고,
② 안쪽이 월×지표 피벗이라 세로로 쌓을 축이 없습니다(같은 `1월` 행이 파일마다 있고, 쌓으면 지사
구분이 행 라벨에 묻힙니다).

**한 줄로 눕히는 쪽을 택했습니다.** 눕히면 지사별 1행이 되어 **기존 합성 모드가 그대로 먹습니다** —
새 모드를 만들지 않았습니다. 시트명을 `총괄표(전개)`로 통일하는 것도 그래서입니다(모드 B가 이름으로
묶습니다). 피벗인지 여부는 이름으로 맞히지 않고 담당자가 고릅니다(F2-17과 같은 이유).

**실측**(분기별 실적 계열 12개): 12파일 → `총괄표(전개)` 한 시트 **13행 × 221열**(헤더 + 지사 12행),
`부서` 컬럼으로 지사가 구분되고 오탐 0건. 안내는 파일당 2건(합계 행 제외·사진 1장 제외)이고 둘 다
의도한 것입니다. 브라우저에서도 3개 파일로 표 선택 → 피벗 지정 → 검토(3파일 정상) → 모드 B 취합
→ 4행 × 221열을 확인했습니다.

**실측이 잡은 것 2건**

| 증상 | 원인 |
|---|---|
| 눕혔는데 컬럼이 `구분`이고 값이 `1월`로 남음 | 라벨 컬럼을 0번으로 단정했다. **실제 총괄표는 B열부터 시작**해 A열 헤더가 비어 있다 → 이름이 있는 첫 컬럼을 라벨로 본다. `_is_summary_or_note`가 `row[0]`만 보다 합계 행을 놓쳤던 것과 같은 종류다 |
| 화면 안내가 '한 시트로 모입니다'라고 단정 | 모드 A는 원본 보존형이라 파일별 시트로 남는다(브라우저에서 실제로 그렇게 나왔다). '모드 B·C로 모을 때 쓰세요'로 고쳤다 |

**한계**: 같은 라벨·컬럼 조합이 두 번 나오면 이름이 겹쳐 `(2)`를 붙입니다(실제 총괄표는 블록이 둘이라
`11월 신고 (2)`가 생깁니다). 조용히 덮는 것보다 낫다고 보고 그대로 뒀습니다. 눕힌 표의 사진은
결과에서 빠집니다 — 어느 칸에 붙어 있었는지 말할 수 없습니다.

### 2단계 AI 토큰 계측·홈 화면 정리 (6차 회차)

**F2 2단계 재검증에 사용량 로깅이 없었습니다.** F1·F6은 `[formgen]`으로 남기는데 실제로 지배적 비용인
구간만 계측이 없어, 취합을 돌린 회차의 토큰을 보고할 수 없었습니다. `_ai_call`에 `[aggregate]` 접두사로
붙였습니다(항목 건수를 함께 찍습니다). Admin 대시보드의 `API 비용`은 **여전히 '미집계'로 둡니다** —
누적 저장 위치를 만들려면 스키마를 늘려야 하고, 그건 물어볼 일입니다. 0으로 단정하지 않는 지금 표기가
사실에 맞습니다.

**홈 화면(화면 2)이 없는 기능을 광고하고 있었습니다.** 목업에서 온 카드 3개 중 '실시간 취합 — 공유된
링크를 통해 데이터가 실시간으로 안전하게 수집됩니다'는 PRD §7이 **범위에서 제외한** 온라인 수집
포털이고, 'B2B 보안 — 기업용 수준의 데이터 암호화'는 근거가 없는 문구였습니다. 실제 기능 3개로
바꾸고 각 화면으로 연결했습니다. 사이드바에 한글 병합·작업 이력을 더하고(주 기능인데 홈에서 닿을 수
없었습니다), 눌러도 아무 일도 없던 로그아웃 버튼과 사번 표시를 다른 화면과 같은 규칙으로 붙였습니다.

### F1-7 대화형 수정 (6차 회차)

**새 프롬프트를 만들지 않았습니다.** "이 항목 빼줘"는 결국 문답 한 턴이고(`intake_turn`은 이미
"사용자가 정정하면 그 지시가 이긴다"를 지시받고 있습니다), 고쳐진 사양으로 저작·검증·변환을
다시 태우면 **사양과 파일이 어긋날 수 없습니다.** 셀을 직접 고치는 쪽은 검증 관문을 하나 더
만들어야 하고, 그 관문을 통과하지 못한 수정본이 사양과 다른 파일로 남습니다. 생성과 수정이
`_author_job` 하나를 공유합니다.

**맞바꾼 것**: 파일을 새로 저작하므로 버전 간 레이아웃이 달라질 수 있습니다. 실측에서 시트명이
`이수현황` → `부서별 월간 안전교육 이수 현황`으로 바뀌었습니다. 레이아웃을 유지해야 한다면
셀 단위 패치가 필요하고 그때는 위의 맞바꿈을 되짚어야 합니다.

**실측**(gpt-5-mini, 브라우저): 양식 생성(2턴) → `비고 빼고 교육시간(시간) 필수로 추가해줘`
→ 모델이 교육시간의 입력 형식을 되물어 **파일을 건드리지 않고** 사양 패널만 갱신 → 형식을
답하니 v2 저작. `form_templates.version=2`, `file_url`이 `_v2.xlsx`, `field_rules`도 5행으로
갈아끼워짐(`비고` 제거·`교육시간(시간)` 추가), **v1 파일은 Storage에 그대로** 남았습니다.
6호출·15.2k 토큰(생성 3 + 수정 3).

수정 실패는 이전 버전을 살려둡니다 — 쓸 수 있던 양식을 `failed`로 덮으면 사용자가 가지고
있던 것까지 잃습니다(`_author_job`의 `version > 1` 분기).

**브라우저 확인이 잡은 결함**: `AI 전송 동의`가 감사 로그에 2건 남았습니다. 동의 API가 이미
동의한 뒤에도 다시 기록해 **동의 시각이 최신 클릭으로 밀렸습니다**(명세 §9.1은 1회 기록).
`ai_consented`를 먼저 보고 건너뜁니다. `test_auth.py`가 시각 불변·로그 1건을 지킵니다.

### F6 기존 양식 인지·등록 (6차 회차)

**설계 요지: 구조 판정기를 새로 만들지 않았습니다.** 헤더·표 인식은 실제 업무 양식 두 계열로
검증된 `aggregate`의 것(`_split_tables`·`_header_block`·`_compose_headers`·`_fill_merged`)을
그대로 씁니다. 두 벌을 만들면 **취합이 보는 헤더와 인지가 보는 헤더가 갈라져** 인지로 만든
작성기준이 취합에서 열을 못 찾습니다. AI가 하는 일은 구조 판정이 아니라 **의도 판정 + 항목별
유형·형식·필수여부 추론**뿐이고, 그것도 한 호출에 합쳤습니다(명세 §6.1이 허용).

스키마는 **컬럼 하나만** 늘렸습니다([20260804060000](supabase/migrations/20260804060000_form_source.sql)).
명세 §3.2는 5개를 요구하지만 `intake_sessions.attachments_json`(F1 첨부용으로 만들어 두고 안
쓰던 컬럼)에 구조 추출 결과와 등록 대상 표시를 담으면 세션 `mode`가 "등록 대상 xlsx가
있는가"로 파생되고, `form_templates.structural_json`은 원본이 무변형으로 남아 있어
`read_structure`로 언제든 다시 뽑습니다(스냅샷을 따로 두면 진실이 둘이 됩니다). `source`만은
파생이 안 됩니다 — `workbook_json is null`로 가르면 저작에 실패한 F1 양식과 등록 양식이 같은
모양이 됩니다.

**실제 파일 실측**(gpt-5-mini, 지사별 자체점검 양식): 첨부 → 의도 `recognize` 판정 → 표
`2025년 대상 리스트`·항목 8개 인식 → 필수 지정 문답 3턴 → 등록 → **바이트 무변경 확인**
(16,868 = 16,868) → 그 작성기준으로 다른 지사 회신본 3개 취합(대구·용인 정상, 청주는 합계 행
안내 1건 = 정탐) → 3파일·3시트·제외 0. `양식 항목 8개 중 8개가 업로드한 파일의 컬럼과
맞았고, 선택 입력 3개를 채웠습니다`가 화면에 떴습니다. **4호출·19.3k 토큰**(4,634 + 5,010 +
4,365 + 5,272).

**실제 파일이 잡은 것 4건** — 합성 fixture로는 드러나지 않는 종류입니다.

| 증상 | 원인 |
|---|---|
| 분기별 실적 계열은 한 파일에 표 6개·헤더 142개 → 전 표를 1:1로 요구하면 인지가 성립하지 않음 | 실제 양식에는 작성 가이드·비워 둔 대장·총괄표가 섞여 있다. F2가 '취합할 표 고르기'로 푼 것과 같은 문제라, **대화로 표를 고르게** 했다(`spec_json.selected_sheets`). 이름 규칙으로 맞히지 않는다 |
| 배포 전 **빈 양식**은 표를 하나도 못 찾음 | `aggregate._find_header`는 헤더 **다음 행에 값이 있을 때만** 헤더로 본다. 취합에서는 맞지만(데이터가 없으면 취합할 것이 없다) 빈 양식은 헤더 아래가 비어 있는 것이 정상이다 → 그 경로만 `_blank_form_headers`로 따로 뒀다 |
| 부서명 값이 통째로 `(성명 마스킹)`으로 가려짐 | 성명 열 판정 목록에 `서명`을 넣었더니 **부서명**이 걸렸다. `담당자 서명`은 담당자로 잡히니 `서명`을 뺐다 |
| 병합된 `지사`·`개소`가 샘플에서 빈 칸으로 보임 | 세로 병합 값 채우기(`_fill_merged`)는 `read_file`이 `_split_tables` **뒤에** 따로 한다. 구조 추출도 통과시켜야 모델이 '선택 항목'으로 오추론하지 않는다 |

**실제 호출이 잡은 결함 2건 — 프롬프트로는 막히지 않아 코드로 막았습니다.**

| 증상 | 원인 |
|---|---|
| 입력 형식에 `General`이 그대로 적힘 | `General`은 표시형식을 지정하지 않았다는 뜻(= 근거 없음)인데 그대로 올려보내니 모델이 입력 형식으로 옮겨 적었다. `None`으로 바꿔 보낸다 |
| 8개 항목을 **전부 '선택 · 확인됨'**으로 내려보냄 | 그대로 등록하면 작성기준이 아무 누락도 잡지 못하는데 화면에는 '확인됨'으로 뜬다. "근거 없으면 물어봐"를 프롬프트에 못 박아도 회차마다 흔들린다 → **근거가 없는데 전 항목의 필수/선택이 똑같으면 그것은 판정이 아니라 기본값**이라고 보고 루브릭에서 막는다. 근거는 헤더의 `*`·`필수` 표시이거나 사용자가 말해준 것이다. 사용자가 지정해 값이 갈리면 자동으로 풀린다(무한 루프 없음) |

`recognize_gaps`가 백엔드 요구사항을 다음 턴 payload(`still_missing`)에도 실어 보냅니다 —
이것 없이는 모델이 "확인할 질문 없습니다"라고 하고 사용자만 gaps 패널을 보게 되어 대화가
수렴하지 않습니다.

**브라우저 확인이 잡은 것**: 문답 중 다른 화면으로 넘어가면 세션이 끊겨 확정된 사양을 두고
문답을 처음부터 다시 해야 했습니다(§5에 적혀 있던 항목). `create.html`이 `GET
/api/form/{sid}`를 쓰도록 붙였고, `?sid=`·sessionStorage 양쪽으로 복원됩니다. 복원 실측:
문답 2턴·첨부·모드·부족 항목이 그대로 되살아났습니다.

**표가 여러 개인 파일 실측**(분기별 실적 계열, 표 6개·헤더 142개): 모델이 추측하지 않고
`회신을 받을 표를 정확한 시트 이름으로 하나만 알려주세요`라고 되물었고, `3. 근로자 의견수렴,
개선`을 지정하니 그 표의 항목 11개만 다뤘습니다. 항목명은 `제안자 소 속`(공백 포함)·
`완료 여부\n(완료/예정)`(줄바꿈 포함)까지 헤더 그대로 나와 취합이 찾는 이름과 일치합니다.
**3턴·33k 토큰**.

**여기서 비용 문제가 드러났습니다** — 표를 고른 뒤에도 표 6개 전부를 올려보내 매 턴 입력이
11.7k였습니다. 고른 뒤로는 나머지를 **이름만** 보내게 바꿨습니다(사용자가 표를 바꿀 수
있어야 해서 이름은 남깁니다). 3번째 턴 입력이 11.7k → **2.8k**(76% 감소)로 줄었고 그 턴에
확정됐습니다.

**등록 대상 지정·작성기준 보정 실측**: xlsx 2개를 붙여 등록 대상을 두 번째로 바꾸고 되돌린
뒤 등록 → 결과 화면에서 `온도차(℃)`를 선택→필수로 눌러 저장 → `GET .../rules`가 필수로
내려왔습니다. **브라우저 확인이 잡은 것**: 등록 대상을 바꾼 파일에 업로드 때 붙인
`참고 자료로만 씁니다` 안내가 남아, 같은 줄에 '등록 대상'과 그 문구가 함께 떴습니다
(`form_mark_primary`에서 안내를 다시 계산합니다).

### hwpx 병합 PoC — SDK 전환 게이트 판정 (F3)

**판정: 상용 SDK로 전환하지 않고 OWPML 직접 병합을 유지한다.** PRD §6.5·§8·결정 로그 #4의 M1 게이트에 대한 답입니다. 근거는 아래 실측입니다. PRD 결정 로그 #4 갱신은 §5의 'PRD v2.0 정리'에 함께 올리세요.

실제 감사 처분요구서 hwpx 3개(같은 양식의 다른 사안, 1개는 사진 3장 포함, `sampledata/hwpx/`, gitignore됨)로 확인했습니다.

| 확인 | 결과 |
|---|---|
| 한글 뷰어에서 열림 | 열림. 첫 쪽이 서식·글꼴·자간·각주 표시까지 원본대로 렌더 |
| 쪽·글자·구역 | `1/12쪽 · 6674글자 · 1/3구역` — 6674자는 구조 검증값과 정확히 일치 |
| 구역 경계 | 5쪽에서 `2/3구역` 시작 — 2번째 문서가 자기 구역으로 제대로 열린다(제목 자간, `권 고`/`통 보` 구분, 제목·관계부서 정렬, 본문 양쪽 정렬 모두 원본 형태) |
| 마지막 구역 | 12쪽 `3/3구역`이 3번째 문서 결말(`[통보] 안전처장은 지사 안전관리관이…` / `처리할 부서 : 안전처 건설안전부`)로 끝남. 굵은 소제목·`[권고]` 인라인 강조·`「」`도 정상 |
| 구역별 본문량 | 병합 결과 구역이 원본 문서와 1:1로 대응(2069·2114·2491자) |
| 사진 화면 렌더 | `[그림 1] 구독형 안전관리 애플리케이션` 캡션과 이미지 2장이 테두리 안에 정상 배치. 겹침·크기 이상 없음 |
| 해석 불가 참조 | 0건 |
| 수량 대조 | 문단 92·표 1·그림 3·글자수 6674가 원본 합계와 일치 |
| 이미지 | `hp:pic` XML이 참조 속성 말고는 원본과 바이트 단위로 동일. 위치·크기 요소(`hp:offset`·`sz`)도 동일 |
| 입력 순서 무관 | 역순으로 병합해도 6674자·구역 3개 동일 |
| 같은 파일 중복 입력 | 3번 넣어도 구역 3개·BinData 9개로 충돌 없이 분리 |

**병합의 실체는 XML 이어붙이기가 아니라 ID 공간 병합입니다.** 3개 파일의 `style`이 모두 0–40, `charPr`가 0–35·0–32·0–33으로 **전부 겹칩니다.** 본문은 이 id를 파일당 100건 넘게 참조합니다. 그래서 문서별 `{옛 id: 새 id}` 매핑을 만들어 항목의 `id`와 모든 참조를 함께 옮깁니다.

실측에서 걸린 함정 4건 — 전부 회귀 테스트로 고정했습니다.

| 증상 | 원인 |
|---|---|
| 병합 header의 `charPr`가 36개(첫 파일 몫)뿐 | 참조(`*IDRef`)만 옮기고 **항목 자신의 `id=`를 안 옮겼다.** 그래서 `id="0"`인 `charPr`가 3개 |
| 뷰어에서 `1/1구역`·첫 파일 몫만 표시 | `hh:head`의 **`secCnt`** 미갱신. `content.hpf` spine에 등록해도 한글은 이 값으로 읽을 구역 수를 정한다. **구조 검증은 전부 통과하는데 화면은 3분의 1만 나오는 상태** — 뷰어로 열어보지 않으면 못 잡았다 |
| `borderFill`의 id가 0이 아니라 **1부터** 시작 | "앞 문서 항목 수만큼 더한다"는 오프셋 산술은 id가 빈틈없이 같은 번호에서 시작할 때만 맞다. 명시적 매핑으로 바꿔 가정을 없앴다 |
| `charPrIDRef="4294967295"` | '없음'을 뜻하는 sentinel(0xFFFFFFFF). id로 착각해 옮기면 서식이 깨진다. 매핑에 없으니 통과시키고, 검증에서도 제외 |

**손대지 않는 참조 4종**: `outlineShapeIDRef`·`memoShapeIDRef`·`linkListIDRef`·`linkListNextIDRef`는 3파일 모두 header에 **정의가 아예 없습니다.** 옮기면 없는 곳을 가리키게 되므로 그대로 두고 건수만 리포트에 밝힙니다.

**남은 한계**(리포트가 매번 함께 출력합니다): `settings.xml`·`version.xml`·`Preview`는 첫 파일 것만 유지됩니다(실측 3개 중 2개가 첫 파일과 다른 `settings.xml`). 파일 경계에서 항상 새 쪽이 시작됩니다(별도 구역 방식의 성질).

**각주 번호는 문서 전체로 이어집니다.** 2번째 문서의 첫 각주가 단독 문서일 때는 `1)`인데 병합본에서는 `3)`으로 나옵니다(1번 문서 각주 2개 뒤). 병합 문서로는 자연스러운 동작이지만 원본과 다른 지점이니 알고 쓰세요. 문서별로 1)부터 다시 시작해야 한다면 각 구역 `secPr`의 각주 시작번호를 손대야 합니다.

**실제 파일 판정은 감사 처분요구서 한 계열에 대한 것입니다.** 사용자가 다른 계열 예시를 줄 수
없다고 해서(5차 회차), 확인하지 못한 계열의 구조를 **합성 fixture로 재현해** 돌렸습니다.

| 계열 | 결과 |
|---|---|
| 원래 여러 구역인 문서 | **이미 동작.** 구역이 파일 수가 아니라 구역 수만큼 늘어난다(2+1=3), `secCnt`·hpf spine 모두 맞다 |
| 머리말·꼬리말 | **이미 동작.** `hp:header`/`hp:footer` 안의 charPr·paraPr 참조도 본문과 똑같이 옮겨진다(안 옮기면 머리말만 남의 서식으로 렌더된다) |
| 15개 규모 | **이미 동작.** 구역 15·id 공간 30·BinData 15, 참조 어긋남 0건 |
| 메모·개체가 든 문서 | **결함이었다 → 실패로 전환.** 아래 참조 |

**조용히 서식이 섞이던 경로를 막았습니다.** `outlineShapeIDRef`·`memoShapeIDRef`를 "header에
정의가 없다"는 실측 근거로 그대로 뒀는데(`UNTOUCHED`), 정의가 **있는** 문서를 병합하면 2번째
문서의 참조가 첫 문서의 정의를 가리켜 서식이 바뀝니다 — 검증도 통과합니다. `hh:refList`의 직계
컨테이너를 세어 아는 7종 밖의 공간에 2번째 이후 문서가 항목을 정의하면 결과 파일을 쓰지
않습니다(`_unmergeable`). 컨테이너 이름을 짐작하지 않는 방식이라 메모든 개체든 새 공간이든
걸립니다. 실측 3개 파일에서는 오탐 없이 그대로 성공합니다(6,674자 일치).

**미검증(남음)**: 실제 메모·개체·머리말 문서로 **뷰어에서 열어본 확인**. 합성 fixture는 구조
검증까지고, 뷰어 렌더는 실제 파일이 있어야 합니다(§4의 `secCnt` 결함이 구조 검증은 전부
통과하는데 화면만 깨졌던 전례). 예시가 생기면 §4 방식대로 원본을 먼저 덤프하세요.

**화면 8 브라우저 실측**(uvicorn + 실제 hwpx 2개, 인증 끔): 업로드 → 목록에 구역 수·용량 표시 → **행을 끌어 순서 뒤집기** → 병합 시작 → 단계·진행률 → 결과(파일 2개·구역 2개·45KB)·한계 안내 → 다운로드. 내려받은 파일의 구역 순서가 화면에서 정한 순서와 정확히 일치했습니다(section0=업무분장 2491자, section1=안전관리 2114자). 라이트·다크 양쪽 렌더 확인, 콘솔 에러 0건.

**브라우저 확인이 잡은 결함**: 드래그 재정렬이 **놓은 행 앞에만** 끼워 넣게 짜여 있어, 아래로 끌면 원래 자리와 같아져 아무 일도 일어나지 않았습니다(`[0,1]`에서 1행을 2행에 놓아도 `[0,1]`). 아래로 끌면 뒤, 위로 끌면 앞에 넣도록 고쳤습니다. `dataTransfer.setData`·`effectAllowed`도 빠져 있어 브라우저에 따라 드래그가 무효로 취급될 수 있었습니다. API 테스트로는 잡히지 않는 종류입니다.

**드래그 검증 방법**: CDP 합성 마우스 이벤트는 HTML5 `dragstart`를 만들지 않아 `left_click_drag`로는 재정렬을 시험할 수 없습니다. 실제 요소에 `DragEvent`(+`DataTransfer`)를 직접 보내 핸들러 경로를 확인했습니다. 손으로 끌어보는 확인은 아직 안 했습니다 — 마우스 제스처→이벤트 변환은 브라우저 표준 동작이라 남은 위험은 낮습니다.

**GUI 자동화 메모**: 한글 뷰어는 갓 띄운 창에서 문서 영역을 한 번 클릭한 뒤에야 합성 키 이벤트를 받습니다. 그 전에는 클릭·PageDown·방향키가 전부 무시됩니다. 스크린샷은 화면 기록 권한, 메뉴·키 조작은 접근성 권한이 각각 필요합니다.

---

## 5. 다음에 할 일

먼저 §0.1의 환경 확인을 돌리세요.

### 외부 조건이 풀려야 되는 것

1. **hwpx 다른 계열 실제 파일 확인** — 메모·개체·머리말이 든 문서는 합성 fixture로만 확인했습니다(§4). 예시 파일이 생기면 §4 방식대로 원본 구조를 먼저 덤프해 엔진 판정과 대조하고, **뷰어에서 열어보는 확인**까지 하세요 (`secCnt` 결함이 구조 검증은 전부 통과하는데 화면만 깨졌던 전례가 있습니다)

2. **마이그레이션 baseline.** 원격 이력의 `0001`·`0002`는 SQL이 저장소에 없어 remote-only입니다. 저장소만으로 DB를 재구축할 수 없다는 뜻입니다. `supabase db pull`이 Docker를 요구하는데 이 머신에 Docker Desktop이 없습니다 — 설치 후 `supabase db pull baseline --linked` 한 번으로 해소됩니다

> **PRD v2.0·design.md v1.1 정리는 끝났습니다**(6차). PRD v2.0에 F1-8(문답 재진입)·F2-16(부서명 지정)·
> F2-17(취합할 표 고르기)·F2-18(한 시트 여러 표·계층 헤더)을 신설하고, F1-3·F1-6·F1-7·F4-3·F6-2~7에
> 구현 확정 사항을 붙였습니다. 결정 로그는 #4(hwpx 전환 게이트 판정: 전환 불필요)를 갱신하고 #28~#31을
> 더했습니다. design.md는 틀렸던 `field_rules.rule_type` 4종을 6종으로, `form_templates` 컬럼과
> 보관·삭제 주체(pg_cron → 백엔드 정리 수단)를 실제 구현에 맞췄습니다.

---

## 6. 알고 있어야 할 상태

### 실제 파일로 쓸 때 반드시 지정할 것

업로드 화면에서 세 가지를 지정하세요. 실제 서식에서 오탐·오작동이 나는 지점은 지금까지 이것들이었습니다.

| 항목 | 미지정 시 |
|---|---|
| 키 컬럼 | 자동 판정(키워드 → 고유 컬럼 → 첫 컬럼). 대개 맞지만 업무상 키가 다르면 지정 |
| 선택 입력 컬럼 | **전 컬럼 필수.** 비고처럼 비워두는 칸이 있으면 그 파일이 '이상'이 되어 취합에서 기본 제외 |
| 부서명 | 파일명 전체가 결과의 `부서` 컬럼에 들어감. 추천 칩을 누르거나 직접 입력 |

CLI에서는 `--rules` JSON의 `"key": true` / `"required": false`로 지정합니다(부서명은 파일명 기준).

### 유료 API 호출

**사용자가 개발 중 실제 OpenAI 호출을 승인했습니다**(2026-08-04, F1 착수 시). 필요할 때 돌리되
**토큰 사용량을 함께 보고**하세요. 규모 감각: `gpt-5-mini`로 양식 생성 한 바퀴가 6호출·16k
토큰·약 $0.022입니다.

전에 거부된 것은 **2단계 AI 병렬화 벤치마크**(§5-4)입니다 — 30개 파일을 실제 키로 돌리는
건이라 규모가 다릅니다. 그건 여전히 별도로 물어보세요.

### Supabase

`chwihap-app` (ref `cfchnprkizcmfymlezyo`, ap-northeast-2 서울), `supabase link` 완료. 계정은 GitHub 연동된 `ksw251020@gmail.com`입니다.

**기존 `ksw1727@gmail.com's Project`는 쓰지 않습니다** — 그 `public`에는 사고 보고 시스템이 19개 테이블로 돌아갑니다. Preview branch는 병합하면 같은 production DB로 들어가 격리가 안 되고, 전용 스키마도 `supabase_migrations` 이력을 공유해 간섭합니다. 별도 프로젝트만이 완전히 분리됩니다.

**RLS 정책은 지금 쓸 일이 아닙니다.** RLS는 켜져 있고 정책이 0개라 `anon`·`authenticated`는 전면 거부이며 모든 접근이 백엔드(service key)를 경유합니다. 공개 키로 조회하면 전 테이블이 빈 배열임을 `test_auth.py`가 지킵니다. 소유자 기반 정책을 쓰면 오히려 `authenticated`에게 읽기를 **열어주는** 셈이라, 프론트가 Supabase를 직접 호출하도록 구조를 바꿀 때 함께 써야 합니다.

**CLI 주의** — `supabase login`이 기본 프로필 토큰을 새 계정으로 덮었습니다(기존 ksw1727 계정은 재로그인 필요). 또 `~/.supabase/profile`이 설정 파일 없는 프로필명(`chwihap`)을 가리켜 `db query`가 `failed to read profile`로 죽길래 `profile.disabled`로 옮겨뒀습니다 — 되돌리려면 파일명만 복구하면 됩니다.

### 계정

지금 계정은 `admin01`(관리자) 하나뿐입니다. 계정 관리는 `/admin.html`에서 하지만 **첫 관리자는 화면에 못 들어가므로** CLI로 올립니다.

```bash
python3 manage_users.py promote admin01
```

이 도구는 비밀번호를 다루지 않습니다 — 가입은 `/login.html`에서 본인이 합니다. 사번은 대소문자를 구분하지 않습니다.

### F1 양식 생성에서 알아둘 것

- **LLM 산출물은 신뢰하지 않습니다.** `validate_workbook`을 통과한 것만 파일이 됩니다. 모델이
  내보내는 키 이름은 회차마다 흔들리므로(`values`/`source`, `integer`/`whole`) 새 변형을 만나면
  `normalize_workbook`에 별칭을 더하세요 — 단 **값을 만들어내지는 마세요**. 목록이 비었으면 비운
  채로 걸리게 두는 것이 맞습니다
- **판단이 필요한 곳에 임의 값을 넣지 않았습니다.** 키 컬럼은 사양에 근거가 없어 비워 두고,
  숫자 범위가 없으면 상·하한을 지어내지 않고 안내만 남기고 몇 건인지 밝힙니다
- 생성 버튼은 **서버가 내린 `spec_complete`로만** 켭니다. 클라이언트가 자체 판정하면 안 됩니다
  (명세 §4.1)
- 문답 저장은 원문, 마스킹은 **외부 전송 직전에만** 합니다(명세 §9.2). 마스커는
  `aggregate.mask` 하나뿐입니다
- **`rule_type` 어휘는 `formgen.FIELD_TYPES`와 DB check 제약 두 곳에 있습니다.** 유형을
  더하면 마이그레이션도 함께 내야 합니다(안 하면 생성이 23514로 죽습니다 — §4). `test_auth.py`가
  두 곳을 대조하니 어긋나면 테스트가 잡습니다
- **Storage 경로는 `{project_id}/` 바로 아래 평면**으로 두세요. 정리 코드가 그 규칙에
  의존합니다(§4)
- 유료 호출은 `[formgen]` 접두사로 토큰 사용량을 stderr에 남깁니다. 규모 감각: 사양을 한 번에
  적으면 2호출·약 5.2k 토큰, 기존 양식 인지는 턴당 약 5k 토큰(등록은 AI를 부르지 않습니다)
- **F6 등록은 원본을 다시 쓰지 않습니다.** 첨부로 올린 Storage 객체를 그대로 양식 파일로 삼기
  때문에 등록 전후 바이트가 같다는 것이 구조적으로 보장됩니다(인지 E1). openpyxl로 열고 다시
  저장하는 코드를 끼우면 조건부서식·매크로가 조용히 사라집니다
- **항목명은 헤더 문자열 그대로**여야 합니다(`부서명*`의 `*`까지). 취합이 이 이름으로 열을
  찾으므로 다듬으면 그 항목 검토가 통째로 빗나갑니다. `recognize_gaps`가 1:1을 강제합니다
- **모델의 필수/선택 판정을 그대로 믿지 마세요.** 근거(헤더의 `*`·사용자의 말)가 없는데 전
  항목이 같은 값이면 루브릭이 막습니다(§4). 이 가드를 풀면 아무 누락도 잡지 못하는 작성기준이
  '확인됨'으로 등록됩니다

### 테스트 데이터 규칙

`test_auth.py`는 실제 Supabase에 계정을 만들고 지웁니다. 사번 접두사는 `zz-test-`이며, teardown이 Storage 폴더와 감사 로그까지 정리합니다. **DB는 계정 1건·로그 0건·버킷 비어 있는 상태로 끝나야 정상입니다** — 남아 있으면 정리가 실패한 것이니 원인을 찾으세요(전에 `audit_logs` FK 때문에 삭제가 막혀 조용히 실패한 적이 있습니다).

실제 업무 파일은 `sampledata/`에 있고 `.gitignore`로 커밋되지 않습니다(지사별 자체점검
엑셀 8개, 분기별 실적 엑셀 12개는 `sampledata/quarterly/`, hwpx 3개는 `sampledata/hwpx/`).

**남아 있는 행이 전부 테스트 흔적은 아닙니다.** 사용자가 `admin01`로 직접 앱을 쓰기도 합니다
— 5차 회차 정리 때 `admin01` 소유 프로젝트 2건·`AI 전송 동의` 로그 1건이 남아 있었고,
실제 업무 파일을 올린 기록이라 **지우지 않았습니다.** 소유자를 확인하기 전에 지우지 마세요.

**감사 로그 액션이 늘었습니다** — F1이 `AI 전송 동의`·`양식 생성 완료`를, F3가 `한글 병합 완료`를
남깁니다. `test_auth.py`의 `_delete_test_logs` 정리 목록에 새 액션을 빠뜨리면 테스트 후 DB에
로그가 남아 §6의 '계정 1건·로그 0건' 규칙이 깨집니다(현재 목록에 8종이 들어 있습니다).

**손으로 만든 테스트 계정도 같은 방식으로 걷어내세요.** 브라우저 확인용으로 API로 계정을 만들면
teardown이 돌지 않습니다. `test_auth._delete_storage_for_owner` → `_delete_user` →
`_delete_test_logs`를 그 user_id로 직접 부르면 됩니다. 5차 회차에 이걸로 정리했고, 그 과정에서
Storage에 양식 6개가 쌓여 있는 것을 발견했습니다(§4).

**Storage는 쓰기 직후 읽기가 즉시 보장되지 않습니다.** 한글 병합 영속화 시나리오가 업로드 직후 그 객체를 읽어 한 번 간헐 실패했습니다(메시지 없는 `AssertionError`로 떠서 원인 찾기 어려웠습니다). 테스트에서 방금 올린 객체를 확인할 때는 `_object_of`처럼 잠깐 기다렸다 다시 읽으세요. 엑셀 시나리오는 업로드와 확인 사이에 검토·취합이 끼어 간격이 벌어져 안 걸렸던 것입니다. 사용자 경로는 세션의 로컬 파일을 주므로 이 지연에 걸리지 않습니다.


### 시계 오차 (6차 회차에 걸림)

**로그인은 되는데 그 뒤 모든 요청이 401이 되는 일이 있습니다.** 원인은 코드가 아니라 시계입니다 — 이
머신이 Supabase보다 3~4초 느려서, 방금 발급된 토큰의 `iat`가 미래로 보이고 PyJWT가
`ImmatureSignatureError`로 거부했습니다. `verify_token`에 `leeway=CLOCK_SKEW_SEC`(60초)를 뒀습니다.
발급자와 검증자의 시계는 언제나 조금 어긋나므로 이 여유는 로컬 사정이 아니라 정상 구성입니다.

**시계가 잘 맞는 머신에서는 이 결함이 드러나지 않습니다.** 그래서 `test_server.py`에 미래 `iat`
토큰을 직접 만들어 태우는 시나리오를 뒀습니다(우리 키로 서명하고 JWKS만 바꿔 끼워 네트워크 없이
돕니다). 여유를 크게 벗어난 토큰은 여전히 거부하는 것도 함께 지킵니다.

### 남은 경계

| 항목 | 상태 |
|---|---|
| 작업 재개 | 재시작 후 기록·산출물은 남지만 중단된 취합을 그 자리에서 이어서 하지는 못함 |
| 진행 중 잡 상태 | `aggregation_jobs`에 남기지만 폴링용 상태는 프로세스 메모리 |
| hwpx 병합(F3) | 전 구간 동작(엔진·API·화면 8·이력). 다른 계열은 합성 fixture로 확인했고, 남은 것은 실제 파일로 뷰어에서 열어보는 확인(§4) |
| AI 양식 생성(F1) | 전 구간 동작. 마이그레이션 반영·브라우저 실측·영속화 회귀 테스트 완료(§4). 첨부(F1-3)·대화형 수정(F1-7) 미착수 |
| 문답 재진입 | 동작(`?sid=`·sessionStorage 복원). 끝난 문답은 되살리지 않는다 |
| 기존 양식 인지·등록(F6) | 전 구간 동작. 실제 업무 양식으로 등록 → 취합까지 실측(§4). 표가 여러 개인 파일의 대화로 표 고르기는 실제 키로 미검증, 첨부 2개 이상일 때 등록 대상 선택 UI 없음 |
| 대화형 수정(F1-7) | 동작. 사양을 고쳐 다시 저작하므로 **버전 간 레이아웃이 달라질 수 있다**(실측에서 시트명이 바뀌었다). 이전 버전 파일은 남는다 |
| 등록 양식 수정 | 원본은 수정 불가가 정책이다(인지 E3, revise가 400으로 막는다). 작성기준은 결과 화면에서 고쳐 저장한다(F6-7) |
| 생성 → 취합 화면 연결 | 동작. 취합 업로드 단계에서 양식을 골라 작성기준을 가져온다(§4) |
| 총괄표류(피벗형) 취합 | 동작(업로드 단계에서 표를 피벗형으로 지정). 같은 라벨·컬럼이 겹치면 `(2)`를 붙이고, 눕힌 표의 사진은 결과에서 빠진다(§4) |
| 사진 미첨부 판정 | 이미지 컬럼은 1단계가 통째로 건너뛰므로 사진을 안 붙인 것은 잡히지 않는다(§4) |
| 보관 기간 정리 | 동작(Admin 버튼 + CLI). **자동 스케줄러는 넣지 않았다** — 세션·잡을 프로세스 메모리에 두는 도구라 재시작이 잦고, 타이머로 무인 삭제를 돌리면 언제 무엇이 지워졌는지 알 수 없다. 주기 실행은 cron이 CLI를 부른다 |
| 이미지 AI Vision · 파일 간 교차 중복 | 명세 §14 v2 백로그 |
