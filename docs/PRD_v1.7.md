# PRD: 취합앱 (자료 취합 전용 프로그램)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.7 |
| 작성일 | 2026-07-22 |
| 상태 | 확정 (v1.6 결정 사항 유지 + F2 검토·취합 세부 스펙 보완 반영) |
| 변경 이력 | v1.1 — F2-3 이상판별을 '검토 결과 출력(UI)' 절차로 재정의, 오류 리포트 다운로드(F2-10) 추가 / v1.2 — 미결 사항 8건 전체 확정 반영 / v1.3 — 합성 모드에 C(그룹 통합형)·D(원본보존+요약형) 추가, 화면 4~7 디자인 시안(목업) 반영 / v1.3 추가 반영 — 로그인 ID를 사번으로 변경(F4-1), 시스템 대시보드 Admin 전용 확정(§3.2), AI 양식 생성에 문답형 정보 수집 절차 추가(F1), 화면 1~3·8~10 목업 결과 반영 / v1.3 최종 반영 — User 홈 화면을 '생성'/'취합' 탭 구조로 확정(F4-4 신규), AI 양식 생성 문답을 항목별 입력 형식까지 확인하는 심층 방식으로 확정(F1-2), 잔여 리스크 노트 2건 해소 / v1.4 — F2 검토(F2-2) 아키텍처를 데이터 유형별(정성적·정량적·이미지) 1차 규칙기반 검증 + 2차 AI(LLM) 재검증의 2단계 구조로 세분화, 이상판별 화면(F2-3) 표시를 정상/이상 이진화로 변경(내부 오류·경고 등급은 F2-4 선택 로직용으로 유지). 세부 기술 스펙은 별도 문서(F2_취합_엑셀_상세정의.md)로 분리 관리 / v1.5 — 생성 기능(F1) 구현 방향 확정 반영: 문답 흐름을 AI 완전 주도로 확정(F1-2), 양식 내용을 AI가 직접 저작하는 아키텍처 확정(§6.5), 출력 포맷 xlsx 우선·hwpx 출력 P1 조정(F1-4), png 첨부 마스킹 예외 규정(§6.2). 기존(외부 확정) 양식을 그대로 사용하는 기능은 F1 범위에서 제외하고 추후 별도 기능(F6)으로 신설 예정. 세부 기술 스펙은 생성기능_기술명세_v0.1.md로 분리 관리. 생성 기능 외 변경 없음 / v1.6 — 결정 로그 #17의 후속: 기존 양식 인지 기능(F6) 요구사항 신설(F6-1~F6-7). 업로드 대상은 빈 양식·예시 데이터 모두 지원, 인지 결과는 F1과 동일한 프로젝트 템플릿(form_templates/field_rules)으로 저장·재사용, 확인 절차는 자동 추론 후 애매한 항목만 최소 확인 질문, 진입 경로는 화면 3의 AI 의도 판정 분기로 확정. 원본 파일은 무변형 등록(수정 불가)하고 작성 기준만 등록 후 보정 가능. F6을 MVP 범위에 편입하고 M3 후반(F1과 함께) 배치 확정. 세부 기술 스펙은 인지기능_기술명세_v0.1.md로 분리 관리 / **v1.7 — F2 상세 스펙(F2_취합_엑셀_상세정의.md) 보완 사항을 PRD에 반영: 2차 AI 재검증 진입 판정을 파일 단위로 확정(파일 내 한 셀이라도 1차 위반이 있으면 해당 파일 전체가 2차 미진입, 부분 통과 없음), 배지 등급에 'AI 재검증 실패'(서비스는 정상이나 개별 항목 판정 불가, 오류 등급) 케이스를 추가해 기존 '정보' 등급의 AI 재검증 생략(서비스 전체 장애)과 구분, 이미지 검토에 anchor 소속범위(병합 셀 불일치 시 소속 모호) 확인 항목 추가, 오류 리포트에 자동교정 이력(원본값→교정값, 감사 추적용) 시트 신설, 오류 등급 항목을 사용자가 강제로 포함해 취합한 경우 결과 화면에 특이사항 안내 신설. 세부 스펙은 F2_취합_엑셀_상세정의.md 참조** |
| 목표 출시 | MVP 3개월 이내 |

---

## 1. 개요

### 1.1 배경
기관 내에서 여러 부서·개인이 작성한 산출물(엑셀, 한글 문서)을 하나로 취합하는 업무는 반복적이고 오류가 잦다. 양식이 통일되지 않은 채 회신된 파일을 담당자가 수작업으로 검토·병합하면서 누락, 중복, 형식 불일치 문제가 상시 발생한다.

### 1.2 목적
취합앱은 **양식 생성(또는 인지) → 회신 파일 검토 → 취합·병합**의 전 과정을 하나의 웹 서비스로 제공하여, 취합 담당자의 수작업을 최소화하고 결과물의 정합성을 보장한다.

### 1.3 핵심 가치
1. 생성형 AI를 활용한 배포용 양식의 신속한 생성과 작성 기준 표준화
2. 검토 결과를 직관적인 화면으로 제시하여 취합 전 문제 파일·문제 값을 사전 식별
3. 엑셀·한글(hwpx) 등 국내 실무 환경에 맞춘 취합·병합 자동화, 부서 특성에 맞는 다양한 합성 방식 지원
4. **(v1.6 신규)** 외부 기관이 이미 확정해 배포한 양식도 원본을 훼손하지 않고 그대로 인지·등록하여, 새로 만든 양식과 동일한 수준의 정합성 판단 기준을 취합 파이프라인에 적용

---

## 2. 제품 전제 (인터뷰 확정 사항)

| 항목 | 결정 사항 |
|---|---|
| 배포 환경 | 클라우드 웹 서비스 (SaaS 형태) |
| 과금 모델 | 내부용 — 과금 없음 |
| 사용 규모 | 기관 단위 중규모, 최대 약 500명 |
| AI 연동 | OpenAI GPT API (벤더 확정) |
| 파일 수집 방식 | 사용자가 수집한 파일을 직접 업로드 (제출 수집 포털은 범위 제외) |
| 인증 | 사번(직원번호)/비밀번호 자체 가입 — 로그인 ID는 사번, 비밀번호 재설정용 이메일은 가입 시 별도 수집. 가입 제한 없음(자유 가입), Admin 사후 통제 |
| 데이터 보관 | 업로드 파일·결과물 90일 보관 (이력 재사용 지원, Admin 정책 변경 가능) |
| MVP 범위 | 4개 기능 동시 출시 **(v1.6: F6 편입으로 3→4개)** |
| 개발 리소스 우선순위 | ① 엑셀 검토·취합 → ② 한글(hwpx) 병합 → ③ AI 양식 생성 → **④ 기존 양식 인지(F6, F1 인프라 확장, v1.6 신규)** |

---

## 3. 사용자 및 권한

### 3.1 사용자 유형
- **User (일반 사용자)**: 취합 실무 담당자. 양식 생성, 파일 업로드, 검토, 취합 실행, 결과 다운로드.
- **Admin (관리자)**: 시스템 운영자. User 기능 전체 + 계정 관리, 사용량 모니터링, 파일 보관 정책 설정, AI API 사용량 관리.

### 3.2 권한 매트릭스

| 기능 | User | Admin |
|---|---|---|
| 회원가입 / 로그인 | ✅ | ✅ |
| AI 양식 생성 / 기존 양식 인지(F6) | ✅ | ✅ |
| 파일 업로드·검토·취합 | ✅ | ✅ |
| 본인 작업 이력 조회·재사용 | ✅ | ✅ |
| 계정 승인·정지·권한 변경 | ❌ | ✅ |
| 보관 기간·용량 정책 설정 | ❌ | ✅ |
| 시스템 사용 현황 대시보드(전체 작업 건수·활성 사용자·API 비용 등) | ❌ | ✅ |
| 전체 사용 로그 조회 | ❌ | ✅ |

---

## 4. 기능 요구사항

### F1. 생성 — AI 기반 배포 양식 생성

생성형 AI(외부 상용 API 연동)를 활용해 배포용 취합 양식을 만드는 기능.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F1-1 | 사용자가 자연어 프롬프트로 만들고자 하는 양식을 최초 요청 | P0 |
| F1-2 | **문답형 정보 수집**: AI가 대상 부서, 포함 항목뿐 아니라 항목별 입력 형식(날짜 표기, 숫자 단위 등)·필수/선택 여부까지 PRD 작성 인터뷰처럼 세부적으로 순차 질문하여 확인. **(v1.5) 질문 내용·순서·사양 완료 판정은 고정 시나리오 없이 AI가 주도하되, 시스템이 필수 사양(양식명·항목 목록·항목별 입력 형식·필수 여부)의 완결성을 별도 검증하여 미비 시 생성을 차단한다.** 수집된 사양은 화면에 실시간으로 표시하고(항목의 추가·수정·삭제 반영), 사양이 충분히 모이기 전에는 생성 버튼을 비활성화. **(v1.5) 진행 상태 표시는 고정 단계 비율이 아닌, 확인된 사양 주제 기반의 근사치로 표시** | P0 |
| F1-3 | 문서 파일(hwpx, xlsx, png 등)을 컨텍스트로 첨부하여 양식 생성 요청. **(v1.5) 이미지(png) 첨부는 자동 마스킹이 적용되지 않음을 고지한 후 허용(§6.2 예외 규정 참조)** | P0 |
| F1-4 | 결과물 렌더링: 배포용 양식 파일로 출력. **(v1.5) xlsx 출력을 우선 구현하고, hwpx 출력은 P1로 조정하여 M3 후반에 배치(MVP 범위 내 유지)** | xlsx P0 / hwpx P1 |
| F1-5 | 양식 생성 시 **데이터별 작성 기준 수립**: 날짜 형식, 이름 형식 등 각 필드의 입력 규칙을 정의하고 양식과 함께 저장 | P0 |
| F1-6 | 생성된 양식·작성 기준을 프로젝트에 저장, 이후 취합 단계(F2-5 전처리)에서 자동 참조 | P1 |
| F1-7 | 대화형 수정: 생성 완료 후 추가 프롬프트로 양식을 반복 수정 (F1-2 문답형 수집과는 별개로, 생성 이후 단계에 적용) | P1 |

**작성 기준(스키마) 예시**
- 날짜: `YYYY-MM-DD`
- 성명: 성+이름 붙여쓰기, 공백 제거
- 금액: 숫자만, 천 단위 구분 없음
- 부서명: 사전 정의된 코드표 기준

**양식 생성 아키텍처 개요 (F1, v1.5 신규)**

- 양식의 내용·레이아웃·서식은 **AI(LLM)가 직접 저작**한다. 시스템은 저작 결과에 대한 안전 검증(구조 한도, 수식 제한 등)과 파일 변환·저장만 담당하며 자체 레이아웃 로직을 갖지 않는다 — 제목행·병합셀·안내문 등 다양한 레이아웃 요구에 대응하기 위한 유연성 확보 목적
- AI 저작 특성상 동일 사양에서도 실행마다 결과가 달라질 수 있으므로, 생성 직후 **미리보기를 필수 노출**하고 대화형 수정(F1-7)으로 교정하는 흐름을 기본 UX로 한다
- 사양 스키마, 검증 규칙, API 설계 등 구현 상세는 별도 문서(**생성기능_기술명세_v0.1.md**)에서 관리

### F2. 취합 — 엑셀

여러 개의 회신 엑셀 파일을 분석(구조 인식)하고 검토·취합하는 핵심 기능.

**처리 파이프라인:** 업로드 → ① 검토 → ② 이상판별(검토 결과 표시) → ③ 범위 확인 → ④ 전처리 → ⑤ 합성 모드 선택 → ⑥ 합성 → 다운로드

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F2-1 | 다중 엑셀 파일 업로드 및 구조 인식(시트, 헤더, 데이터 영역 자동 감지) | P0 |
| F2-2 | **검토**: 파일별로 정합성·누락·중복·충돌 여부 자동 점검. 데이터 유형(정성적·정량적·이미지)별로 1차 규칙 기반 검증을 수행하고, 정성적·정량적 데이터는 1차 통과 시 2차 AI(LLM) 재검증까지 거쳐 최종 판정한다(이미지는 1차 규칙 검증만 수행). **(v1.7) 2차 진입 판정은 파일 단위로 이루어지며, 파일 내 한 셀이라도 1차 위반이 있으면 해당 파일 전체가 2차로 진입하지 않는다(부분 통과 없음)**. 상세 판정 기준·알고리즘은 별도 문서(F2_취합_엑셀_상세정의.md) 참조 | P0 |
| F2-3 | **이상판별(검토 결과 표시)**: F2-2 검토에서 발생한 이상값을 파일 리스트 + 상태 표시(정상/이상 이진화) UI로 사용자에게 출력. 상태값에 마우스오버·클릭 시 문제 위치(파일명·시트명·셀 위치)와 사유를 노출 (상세 명세는 아래 참조) | P0 |
| F2-4 | **취합 진행 여부 확인 UI**: F2-3의 파일 리스트에서 체크박스로 취합 범위를 선택. 체크박스 선택/해제에 따라 "선택된 파일 n/m건"과 하단 진행 버튼 "취합을 진행하시겠습니까 (n/m)" 문구가 실시간으로 갱신됨 | P0 |
| F2-5 | **데이터 전처리**: 생성 단계의 작성 기준이 있으면 준용, 없으면 사용자가 데이터별 기준을 설정하여 형식을 통일. **(v1.6) F1이 생성한 양식뿐 아니라 F6으로 등록된 기존(외부 확정) 양식의 작성 기준도 동일한 방식으로 자동 준용** | P0 |
| F2-6 | **합성 모드 A — 원본 보존형**: 부서별 시트를 원본 그대로 유지하며 하나의 파일로 합성 (결과 시트 개수 = n×m, 부서 수×시트 수) | P0 |
| F2-7 | **합성 모드 B — 시트명 통합형**: 시트명이 동일한 시트끼리 부서별 데이터를 세로로 누적, "부서" 구분 컬럼 추가 (결과 시트 개수 = m) | P0 |
| F2-8 | **합성 모드 C — 그룹(카테고리) 통합형**: 사전 정의된 그룹 매핑(시트명→그룹명)에 따라 여러 시트를 그룹 단위로 재편성해 통합, "구분"(원 시트명)·"부서" 컬럼 추가 (결과 시트 개수 = k, 그룹 수) | P1 |
| F2-9 | **합성 모드 D — 원본보존+요약형**: 모드 B의 결과 구조를 유지하며, 부서별 핵심 지표를 SUMIF/SUMIFS 수식으로 집계하는 "종합요약" 시트를 최상단에 추가 (결과 시트 개수 = m+1) | P1 |
| F2-10 | 확장 합성 모드 — v2 백로그: ① 키 기준 병합(동일 키 행 매칭 후 열 결합), ② 요약 집계(피벗형), ③ 세로 누적 → 가로 전개 변환. 출시 후 사용자 피드백으로 케이스 추가 수집 | P2 |
| F2-11 | 취합 결과 리포트: 처리 건수, 제외 파일, 수정된 항목 요약 제공. **(v1.7) 오류 등급 항목을 사용자가 강제로 포함해 취합한 경우, 결과 미리보기 화면에 특이사항 안내(어떤 항목이 강제 포함됐는지, 수식 오류 가능성 등)를 노출** | P1 |
| F2-12 | **오류 리포트 다운로드(반려 회신용)**: 파일별 오류 내역(유형·위치·사유)을 문서로 내려받아 제출자에게 수정 요청 시 활용. **(v1.7) 자동교정 이력(원본값→교정값) 시트를 추가해 감사 추적 지원** | P0 |

**합성 모드 상세 정의 (F2-6~F2-9)**

| 모드 | 결과 구성 | 추가 컬럼 | 필요 입력 | 주요 예외 처리 |
|---|---|---|---|---|
| A. 원본 보존형 | {부서명}_{원본시트명} 시트 n×m개 | 없음 (원본 그대로) | 없음 | 시트명 31자 초과 시 자르기 규칙 적용 |
| B. 시트명 통합형 | 동일 시트명 기준 시트 m개 | 부서 | 없음 | 부서 간 헤더 불일치 시 오류 시트로 분리 |
| C. 그룹 통합형 | 그룹 매핑 기준 시트 k개 | 구분(원 시트명), 부서 | 그룹 매핑(시트명→그룹명) | 매핑에 없는 시트는 "미분류" 그룹 자동 편입 후 경고 |
| D. 원본보존+요약형 | 모드 B 구성(m개) + 종합요약 시트 1개 | 모드 B와 동일 + 요약 시트 지표 합계 | 요약 대상 지표(컬럼) | 지정 컬럼 부재 시 "N/A" 처리, 수치는 수식 기반(하드코딩 금지). **(v1.7) 사용자가 오류 등급 데이터를 강제 포함한 경우 수식 오류(`#VALUE!` 등)가 그대로 노출될 수 있음(별도 차단 없이 사용자 확인·수정 유도, F2-11 특이사항 안내와 연동)** |

**검토 아키텍처 개요 (F2-2, v1.4 신규)**

- 정성적(텍스트)·정량적(숫자) 데이터: **1차 규칙 기반 알고리즘 검증** → 통과 시에만 **2차 AI(LLM) 재검증**을 거쳐 최종 판정. 어느 단계에서든 이상이 발견되면 즉시 확정하고 다음 단계로 넘어가지 않는다. **(v1.7) 1차 통과 판정은 파일 단위로 이루어진다 — 파일 내 단 하나의 셀이라도 1차 위반이 있으면 그 파일 전체가 2차로 진입하지 않는다(부분 통과 없음, 불필요한 API 호출 방지 목적)**
- 이미지 데이터: 1차 규칙 기반 검증(확장자·용량·해상도·중복 등 구조/메타데이터 체크)만 수행하고 종료. **(v1.7) 이미지가 병합 셀 영역과 일치하지 않은 채 여러 데이터 행에 걸쳐 있으면 소속 레코드가 모호한 것으로 보아 경고로 표시**. 내용 기반 AI 분석(Vision)은 범위 밖(v2 검토)
- 이 구조는 §6.4의 "AI 장애 무관 정상 동작" 원칙에 예외를 발생시키며, 상세는 §6.4 및 잔여 리스크 노트 참조
- 판정 알고리즘, 데이터 유형별 세부 기준, 임계값, AI 프롬프트 설계 등 구현 상세는 팀원별 개발 담당 분야에 맞춰 별도 문서(**F2_취합_엑셀_상세정의.md**)에서 관리하며, 필요 시 유사한 상세 문서를 F1·F3에도 확장할 예정

**이상판별 UI 명세 (F2-3) — 파일 리스트 + 상태 표시 방식**

기획 원안의 성공/실패 체크리스트 UI를 확장한 형태로, 검토 결과 확인과 취합 범위 선택(F2-4)이 한 화면에서 이루어진다.

1. **파일 리스트**: 업로드된 파일을 행 단위로 나열. 각 행 = 체크박스(취합 포함 여부) + 파일명 + 상태 표시
2. **상태 표시 — 정상/이상 이진화 (v1.4)**: 화면에는 `정상` 또는 `이상` 두 가지만 표시한다. 기존의 정상/경고/오류 3단계 구분은 화면에 노출하지 않고 **내부 판정 등급**으로 유지하여 F2-4의 기본 선택 로직에만 사용한다(아래 "배지 등급 분류 기준" 참조)
3. **상세 노출(마우스오버/클릭)**: `이상` 상태값에 마우스오버 또는 클릭 시 해당 파일의 문제 상세를 노출 — 오류 유형(정합성/누락/중복/충돌, **이미지 전용 소속범위**, **AI 재검증 실패**(v1.7)), 위치(시트·셀 좌표), 사유를 항목별로 표시
4. **기본 선택 상태**: 내부 등급 기준 `정상`·`경고` 파일은 체크, `오류` 파일은 체크 해제 상태로 초기 표시(사용자가 변경 가능)
5. **오류 리포트 다운로드(F2-12)**: 화면 상단에 다운로드 버튼 제공. 파일별 또는 전체 오류 내역을 xlsx 리포트로 생성 — 제출자에게 반려·수정 요청 회신 시 그대로 첨부 가능한 형식(파일명, 시트, 셀 위치, 오류 유형, 사유, 작성 기준 열 포함)
6. **하단 진행 버튼**: "취합을 진행하시겠습니까 (선택/전체)" 형태로 선택 건수 실시간 반영

**배지 등급 분류 기준 (확정, v1.4부터 내부 판정 등급으로 사용)**

| 등급 | 분류 기준 | 해당 검토 항목 | 기본 체크 |
|---|---|---|---|
| 오류 | 취합 결과를 훼손하는 문제 | 필수값 누락, 동일 키 충돌(상이 값), 구조 불일치(헤더·시트 인식 실패), **AI 재검증 실패**(서비스는 정상이나 개별 항목 판정 불가, v1.7 신규) | 해제 |
| 경고 | 자동 처리 가능하거나 경미한 문제(AI 재검증에서 발견된 이상 포함) | 자동 변환 가능한 형식 불일치(날짜 표기 변형 등), 완전 중복 행(자동 제거 가능), 비필수 필드 누락, 공백·대소문자 위반, 이미지 소속 범위 모호(v1.7 신규) | 유지 |
| 정보 | 문제 아님, 안내 목적 | 전처리에서 자동 수정된 항목 내역, **서비스 전체 장애**로 인한 AI 재검증 생략("AI 미검증") 안내(**v1.7: 개별 항목만의 판정 실패는 오류로 분류되며 이 정보 등급과는 다름**) | 유지 |

**검토 항목 정의 (F2-2)**

| 항목 | 정의 | 예시 |
|---|---|---|
| 정합성 | 작성 기준(형식·타입) 위반 | 날짜 칸에 "미정" 입력 |
| 누락 | 필수 필드 미입력, 파일·시트 누락 | 성명 공란 |
| 중복 | 동일 키 데이터의 중복 제출 | 같은 사번 2회 등장 |
| 충돌 | 동일 키에 서로 다른 값 존재 | 동일인의 부서가 파일마다 다름 |

### F3. 취합 — 한글 (hwpx)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F3-1 | 다중 hwpx 파일 업로드 | P0 |
| F3-2 | 드래그 앤 드롭으로 병합 순서 지정 | P0 |
| F3-3 | 서버 사이드 병합 후 결과 파일 다운로드 | P0 |
| F3-4 | 대용량 파일 병합은 **비동기 작업**으로 처리(작업 큐), 진행 상태 표시 및 완료 알림 | P0 |
| F3-5 | 임시 파일 자동 정리 | P0 |
| F3-6 | 업로드 용량 제한 적용(§6 참조) 및 초과 시 안내 | P0 |

### F4. 공통 — 계정·이력 관리

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F4-1 | **사번(직원번호)/비밀번호 회원가입**(가입 제한 없음): 로그인 ID는 사번을 사용하고, 비밀번호 재설정 인증용 이메일은 가입 시 별도로 수집. Admin이 계정 정지·삭제로 사후 통제 | P0 |
| F4-2 | 작업 이력: 과거 업로드 파일·양식·취합 결과를 보관 기간 내 조회 및 재사용. User는 본인 작업 이력만 조회 가능 | P1 |
| F4-3 | **Admin 콘솔**: 계정 승인·정지, 시스템 사용 현황 대시보드(전체 작업 건수·활성 사용자·API 비용·보관 용량), 보관·업로드 정책 설정. User에게는 노출되지 않는 Admin 전용 화면 | P1 |
| F4-4 | **User 홈 화면(신규)**: 로그인 직후 진입하는 기본 화면. 상단에 '생성'/'취합' 탭 바를 구성하고 기본 선택 탭은 '생성'. '생성' 탭은 AI 양식 생성 컨텍스트 화면(F1)으로, '취합' 탭은 엑셀 업로드 화면(F2-1)으로 연결 | P0 |

### F6. 인지 — 기존 양식 인지·등록 (v1.6 신규)

외부 기관 등에서 이미 확정되어 배포된 양식을 시스템이 그대로 인지하여, 별도의 재저작 없이 취합 파이프라인(F2)에서도 정합성 판단 기준이 동일하게 적용되도록 하는 기능. 결정 로그 #17의 후속 확정 사항.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F6-1 | 화면 3(AI 양식 생성 문답 화면) 대화창에서 사용자의 발화·첨부로부터 "이미 정해진 외부 양식을 그대로 쓰겠다"는 의도를 AI가 판정해 인지·등록 플로우로 자동 분기. 의도가 모호하면 "새 양식 만들기"/"첨부 파일 그대로 등록" 선택지를 제시해 재확인 | P0 |
| F6-2 | 외부 양식 파일 업로드: xlsx는 완전 지원(빈 양식·예시 데이터 포함 파일 모두 허용), hwpx는 참고 첨부로만 허용(등록 대상 아님), png는 미지원 | P0 |
| F6-3 | **구조 인지**: 업로드된 xlsx의 시트·헤더·데이터 영역·기존 서식(날짜 형식 등)·기존 유효성 검사(드롭다운 목록 등)를 구조적으로 추출하고, 이를 근거로 필드별 타입·형식·필수 여부를 AI가 추론 | P0 |
| F6-4 | **확인·보정**: 추론 결과를 필드별로 제시하고, 확신도가 낮거나 정보가 불충분한 항목만 최소한의 확인 질문으로 보완. 등록을 확정하기 전 사용자가 임의 필드를 직접 수정 가능 | P0 |
| F6-5 | **등록 확정**: 사용자가 확정하면 원본 파일을 무변경 상태로 저장하고(재저작·레이아웃 변환 없음), 확정된 필드 정보를 작성 기준(F1-5의 field_rules와 동일 구조)으로 저장 | P0 |
| F6-6 | **취합 연동**: 등록된 양식·작성기준은 F2-5 전처리에서 F1이 생성한 양식과 동일한 방식으로 자동 참조되어, 외부에서 정해진 양식으로 수집한 회신 파일의 취합에도 정합성 판단이 동일하게 적용됨 | P0 |
| F6-7 | **등록 후 보정**: 등록된 원본 파일 자체는 이후 수정(재저작) 불가하나, 추출된 작성 기준은 등록 후에도 별도로 재확인·수정 가능. 원본 양식 자체가 개정된 경우 재인지(새 버전 등록)로 처리 | P1 |

> ID는 F5를 건너뛰고 F6으로 부여한다(결정 로그 #17에서부터 사용해 온 번호를 유지).

**인지 기능 아키텍처 개요 (F6, v1.6 신규)**

- 업로드된 원본 파일은 **AI가 재저작하지 않고 그대로 보존**한다 — F1의 양식 생성(내용을 AI가 직접 저작)과 반대로, F6은 이미 확정된 외부 양식의 레이아웃·서식을 훼손하지 않는 것이 핵심 목적이다
- AI의 역할은 파일의 **구조를 인지하여 정합성 판단 기준(필드별 타입·형식·필수여부)을 추출**하는 것으로 한정되며, 추출 결과는 사용자 확인·보정을 거쳐 확정된다
- 진입 경로는 별도 화면이 아니라 F1과 동일한 화면 3(AI 양식 생성 문답 화면)이며, 사용자의 발화·첨부 의도를 AI가 판정해 생성/인지 플로우로 자동 분기한다
- F1의 데이터 모델(`intake_sessions`, `form_templates`, `field_rules`)을 그대로 확장해 재사용하며, 등록된 원본 파일은 F1 생성 양식과 동일하게 프로젝트에 저장되어 F2 취합에서 재사용된다
- 구현 상세는 별도 문서(**인지기능_기술명세_v0.1.md**)에서 관리

---

## 5. 사용자 시나리오 (핵심 플로우)

1. **양식 배포 준비** — 담당자 A가 로그인 후 "부서별 예산 집행 현황 양식 만들어줘"라고 프롬프트 입력, 기존 hwpx 참고 파일 첨부 → AI가 대상 부서·항목·입력 형식을 문답으로 확인 → xlsx 양식과 작성 기준 생성 → 미리보기 확인 후 필요 시 대화형 수정 → 다운로드하여 각 부서에 배포(배포 자체는 앱 외부에서 수행).
2. **회신 취합** — 부서별 회신 파일 10개를 수집하여 일괄 업로드 → 시스템이 1차 규칙 검증과 2차 AI 재검증을 거쳐 검토 실행 → 파일 리스트에 정상/이상 상태로 표시(정상 7 / 이상 3), 이상 파일 3개의 상태값에 마우스오버하여 날짜 형식 오류·중복 행 상세(파일명·시트·셀 위치) 확인 → 오류 리포트를 다운로드해 해당 부서에 수정 요청 회신 → 우선 문제없는 7개만 체크(선택 건수 실시간 반영) → "취합을 진행하시겠습니까 (7/10)" 확인 → 전처리(작성 기준 자동 준용) → 합성 모드 선택 화면에서 시트명 통합형(모드 B)을 선택하고 결과 미리보기로 산출 형태 확인 → 합성 → 결과 다운로드.
3. **한글 병합** — 부서별 보고서 hwpx 15개 업로드 → 드래그로 목차 순서 지정 → 서버 비동기 병합 → 완료 알림 후 다운로드.
4. **기존 양식 인지·등록 (v1.6 신규)** — 담당자 B가 화면 3에서 "이 양식 그대로 써야 해요"라며 외부 기관에서 받은 확정 예산 서식(xlsx, 일부 예시 데이터 포함)을 첨부 → AI가 의도를 인지하여 등록 플로우로 분기 → 구조를 인지해 부서명·집행일자·집행금액 등 필드의 타입·형식·필수 여부를 자동 추출 → 확신도가 낮은 "비고" 필드의 필수 여부만 확인 질문 → 답변 후 "이대로 등록하기" 클릭 → 원본 파일을 무변경 등록하고 작성 기준 저장 → 이후 이 양식으로 수집한 회신 파일들을 F2(취합)에 업로드하면 등록된 작성 기준이 자동 준용되어 정합성 검토가 수행됨.

---

## 6. 비기능 요구사항

### 6.1 성능·용량
- 동시 사용자: 최대 500명 등록 기준, 동시 접속 50명 수준 무리 없이 처리
- 업로드 제한(확정): 파일당 50MB, 1회 취합 작업당 최대 30개 파일 / 총 500MB. 초과 시 안내 메시지 표시
- 엑셀 취합(파일 10개, 총 10만 행 이내): 동기 처리 3분 이내 목표, 초과 시 비동기 전환
- hwpx 병합: 기준 초과 시 비동기 작업 큐 처리, 진행률 표시

### 6.2 보안
- 전 구간 HTTPS(TLS 1.2+), 저장 데이터 암호화(at-rest)
- 비밀번호 해시 저장(bcrypt/argon2), 로그인 시도 제한
- **외부 AI API 전송 정책(확정)**: OpenAI GPT API 사용. 문서 데이터가 외부 API로 전송됨을 사용자에게 고지하고 동의 절차 마련. API 데이터 학습 미사용(zero data retention) 조건 적용. F2-2의 2차 AI 재검증에 전송되는 셀 값에도 동일한 마스킹·동의 정책을 적용
- **개인정보 자동 마스킹(확정)**: 외부 API 전송 전 패턴 기반(정규식) 마스킹 적용 — 주민등록번호·전화번호·이메일은 자동 치환, 성명 열은 사용자가 마스킹 여부 선택. AI 기반 개인정보 탐지는 v2 검토. **(v1.5 예외 규정)** 양식 생성 첨부 중 이미지(png)는 패턴 기반 마스킹을 적용할 수 없으므로, AI 전송 동의 절차에 "이미지는 자동 마스킹이 적용되지 않음"을 명시적으로 고지하고 개인정보가 포함된 이미지 첨부를 지양하도록 안내한 후 허용한다(F1-3)
- **F6 등록 파일 보안(v1.6 신규)**: 등록 대상 파일은 매크로 포함 확장자(xlsm 등)를 거부하고 매직바이트 검증을 거친다. 등록된 원본 파일은 재저작하지 않고 무변형 보존한다. 인지 대상으로 예시(샘플) 데이터가 포함된 실제 회신 파일이 업로드될 수 있어 F1의 일반 참고 첨부보다 개인정보 노출 위험이 높으므로, F6에서는 성명 열 마스킹 기본값을 "마스킹함"으로 전환한다
- **자유 가입 방침에 따른 통제**: 가입 자체는 제한하지 않되, Admin이 신규 가입 알림·계정 정지·삭제 권한으로 사후 통제하고 전체 사용 로그를 기록
- 사용자별 데이터 격리(타 사용자 파일 접근 불가), Admin 접근 로그 기록

### 6.3 데이터 보관
- 업로드 파일·결과물은 **90일 보관 후 자동 삭제(확정)**, Admin이 정책 변경 가능
- 사용자 수동 삭제 지원, 병합용 임시 파일은 작업 완료 즉시 삭제

### 6.4 가용성·운영
- 클라우드 인프라 기반, 목표 가용성 99.5%
- AI API 장애 시: 양식 생성(F1)·인지(F6)는 기능 저하 모드로 안내. 엑셀 검토(F2-2)의 1차 규칙 기반 검증과 이상판별·취합(F2), 한글 병합(F3)은 AI 장애와 무관하게 정상 동작을 유지한다. **단, F2-2의 2차 AI 재검증(정성적·정량적 데이터 한정)은 장애 시 자동 생략되며, 해당 항목은 1차 검증 결과만으로 "정상(AI 미검증)"으로 표시되어 취합 진행이 차단되지 않는다(v1.4 예외 규정, 상세는 F2_취합_엑셀_상세정의.md)**. **(v1.7) 단, 서비스는 정상이나 개별 항목의 AI 응답 판정만 실패하는 경우는 이 예외와 별개로 "오류" 등급으로 분류되어 취합 시 사용자 확인이 필요하다**

### 6.5 기술 결정 사항
- **AI 벤더**: OpenAI GPT API. 단, API 호출부는 어댑터 계층으로 분리 구현하여 추후 벤더 교체 가능성 확보. F2-2의 2차 AI 재검증도 동일 어댑터·벤더를 사용
- **hwpx 처리**: hwpx는 ZIP+XML(OWPML) 개방형 포맷이므로 직접 파싱·병합 방식으로 개발. M1에서 PoC를 수행하고, 복잡 서식(개체·스타일 충돌) 병합 품질이 기준 미달일 경우 상용 SDK(한컴 등) 전환 여부를 M1 종료 시점에 판단(전환 게이트)
- **양식 생성 저작 주체(v1.5 신규)**: 생성(F1)의 양식 내용·레이아웃·서식은 AI가 직접 저작하고, 시스템은 저작 결과의 안전 검증과 파일 변환·저장만 담당한다(자체 레이아웃 로직 없음). 상세는 생성기능_기술명세_v0.1.md 참조
- **F6 인지 아키텍처(v1.6 신규)**: 등록 대상 원본 파일은 AI가 재저작하지 않고 무변형 보존하며, AI는 구조 인지 및 작성 기준(field_rules) 추출까지만 담당한다. 등록(register)은 F1 생성(generate)과 달리 동기 처리로 구현한다(LLM이 파일을 저작하지 않아 처리 시간이 짧다는 전제). 상세는 인지기능_기술명세_v0.1.md 참조

---

## 7. 범위 제외 (Out of Scope — MVP)

- 제출자용 온라인 수집 포털(링크 발송·온라인 제출) — 직접 업로드 방식만 지원
- 과금·결제 기능
- SSO 연동(자체 가입만 지원, 추후 검토)
- hwp(구버전 바이너리) 형식 — hwpx만 지원
- 모바일 전용 앱 — 반응형 웹으로 대응
- **매크로 포함 엑셀(.xlsm) 등록 — F6 보안 정책상 제외(v1.6 신규, §6.2)**

---

## 8. 마일스톤 (3개월)

| 기간 | 목표 |
|---|---|
| M1 (1~4주) | 계정/인증, 파일 업로드 인프라, 엑셀 구조 인식 및 규칙 기반 검토(F2-1~2), hwpx OWPML 직접 파싱·병합 PoC → 종료 시점에 상용 SDK 전환 게이트 판단 |
| M2 (5~8주) | 이상판별 UI·오류 리포트 다운로드(F2-3, F2-12), 취합 범위 확인(F2-4), 전처리·합성 모드 A/B/C/D(F2-5~9), F2-2 2차 AI 재검증, hwpx 병합 + 비동기 처리(F3) |
| M3 (9~12주) | AI 양식 생성(F1 — xlsx 출력 우선, hwpx 출력(P1)은 M3 후반 배치), **기존 양식 인지·등록(F6 — F1 인프라 확장, M3 후반 F1과 함께 배치, v1.6 신규)**, 작업 이력, Admin 콘솔, 보안 점검, 통합 테스트 및 출시 |

---

## 9. 성공 지표 (초안)

- 취합 작업 1건당 소요 시간: 수작업 대비 70% 이상 단축
- 검토 탐지 정확도: 규칙 기반 검토(F2-2)의 오류 탐지 재현율 90% 이상 (내부 테스트셋 기준)
- 취합 결과 재작업률: 10% 미만
- 출시 3개월 내 기관 내 활성 사용자 100명 이상

---

## 10. 결정 사항 로그 (Resolved)

| # | 항목 | 결정 내용 | 반영 위치 |
|---|---|---|---|
| 1 | 업로드 제한 | 파일당 50MB, 작업당 최대 30개 / 총 500MB 확정 | §6.1 |
| 2 | AI API 벤더 | OpenAI GPT API 확정, 어댑터 계층으로 벤더 교체 가능성 확보 | §6.5 |
| 3 | 개인정보 마스킹 | 패턴 기반 자동 마스킹 적용(주민번호·전화·이메일 자동, 성명은 사용자 선택), AI 기반 탐지는 v2 | §6.2 |
| 4 | hwpx 기술 스택 | OWPML 직접 파싱·병합, M1 PoC 후 상용 SDK 전환 게이트 | §6.5, §8 |
| 5 | 보관 기간 | 기본 90일, Admin 정책 변경 가능 | §6.3 |
| 6 | 확장 합성 모드 | 모드 C(그룹 통합형)·D(원본보존+요약형)는 MVP로 편입(P1). 키 기준 병합·요약 집계·세로→가로 전개 3종은 v2 백로그로 유지 | F2-8, F2-9, F2-10 |
| 7 | 가입 통제 | 제한 없음(자유 가입), Admin 사후 통제(가입 알림·계정 정지·삭제)로 보완 | §6.2, F4-1 |
| 8 | 배지 등급 분류 | 오류/경고/정보 3등급 기준표 확정 | F2-3 |
| 9 | 로그인 ID 체계 | 사번(직원번호)을 로그인 ID로 사용. 비밀번호 재설정용 이메일은 회원가입 시 별도로 수집 | §2, F4-1, 화면 1 |
| 10 | 대시보드 접근 권한 | 시스템 사용 현황 대시보드(전체 작업·활성 사용자·API 비용 등)는 Admin 전용으로 확정, 일반 User에게는 노출하지 않으며 화면 10(Admin 콘솔) 하나로 통합 | §3.2, F4-3, 화면 10 |
| 11 | AI 양식 생성 방식 | 단발성 프롬프트 생성 대신, 대상 부서·포함 항목·항목별 입력 형식·필수여부까지 PRD 인터뷰 방식으로 확인하는 문답형(챗봇) 정보 수집 절차를 거쳐 생성. 사양이 모두 확인되기 전에는 생성 버튼 비활성화 | F1-2, 화면 3 |
| 12 | 일반 User 랜딩 화면 | 로그인 후 기본 화면은 상단 탭바('생성'/'취합')로 구성. 기본 탭은 '생성'(AI 양식 생성 문답 화면), '취합' 탭 선택 시 엑셀 업로드 화면(F2-1)으로 전환 | F4-4(신규), 화면 2 |
| 13 | F2 검토 아키텍처 개편 | 데이터 유형(정성적·정량적·이미지)별 1차 규칙기반 검증 + 2차 AI(LLM) 재검증(정성·정량 한정, 이미지는 1차만 수행)의 2단계 구조 확정. 이상판별 화면(F2-3) 표시는 정상/이상 이진화하고, 오류/경고 등급은 내부 판정으로 유지해 F2-4 기본 선택에 사용. §6.4 AI 무관 동작 보장에 예외 조항 추가. 세부 스펙은 F2_취합_엑셀_상세정의.md에서 별도 관리 | F2-2, F2-3, §6.4, F2_취합_엑셀_상세정의.md |
| 14 | F1 문답 흐름 제어(v1.5) | 질문 내용·순서·사양 완료 판정을 고정 시나리오 없이 AI가 완전 주도. 단, 필수 사양 완결성은 시스템이 별도 검증하여 미비 시 생성 차단(이중 안전장치). 진행 상태 표시는 확인된 사양 주제 기반 근사치로 변경 | F1-2, 화면 3 |
| 15 | F1 양식 저작 주체(v1.5) | 양식 내용·레이아웃·서식을 AI가 직접 저작, 시스템은 안전 검증·파일 변환만 담당. 다양한 레이아웃 요구 대응 유연성이 결정 근거 | §6.5 |
| 16 | F1 출력 포맷 우선순위(v1.5) | xlsx 출력 P0 우선 구현, hwpx 출력은 P1로 조정하되 MVP 범위 내 유지(M3 후반 배치) | F1-4, §8 |
| 17 | 기존 양식 사용 기능 분리(v1.5) | 외부 기관 등에서 확정되어 온 기존 양식을 그대로 사용하는 기능은 F1(생성) 범위에서 제외하고, 추후 별도 기능 F6로 신설하기로 결정. F6 요구사항 정의 시점에 상세(등록 방식·작성 기준 확보·수정 정책 등)를 확정 | F6, §12 |
| 18 | png 첨부 마스킹 예외(v1.5) | 이미지(png)는 패턴 기반 마스킹 적용 불가 → 동의 절차에 미적용 사실을 고지한 후 허용 | §6.2, F1-3 |
| 19 | F6 MVP 편입 및 마일스톤 확정(v1.6) | 기존 양식 인지 기능(F6)을 MVP 범위에 포함하기로 확정. F1 인프라(intake_sessions/form_templates/field_rules)를 확장 재사용하므로 추가 구현 비용이 크지 않다고 판단해 M3 후반, F1과 함께 개발한다. F6-1~F6-6은 P0, 등록 후 보정(F6-7)은 P1로 우선순위 부여 | F6, §8 |
| 20 | F6 요구사항 및 아키텍처 확정(v1.6) | 업로드 대상은 빈 양식·예시 데이터 모두 지원, 인지 결과는 프로젝트 템플릿(form_templates/field_rules)으로 저장·재사용, 확인 절차는 자동 추론 후 애매한 항목만 최소 확인 질문, 진입 경로는 화면 3 내 AI 의도 판정 분기로 확정. 원본 파일은 무변형 등록(수정 불가), 작성 기준만 등록 후 보정 가능. 상세는 인지기능_기술명세_v0.1.md 참조 | F6, §12 |
| 21 | F2 검토·취합 세부 스펙 보완(v1.7) | 2차 AI 재검증 진입 판정을 파일 단위로 확정(파일 내 한 셀이라도 1차 위반 시 해당 파일 전체 2차 미진입, 부분 통과 없음). 배지 등급에 "AI 재검증 실패"(서비스는 정상이나 개별 항목 판정 불가, 오류 등급) 케이스를 추가해 기존 서비스 전체 장애("정보" 등급, AI 미검증)와 구분. 이미지 검토에 anchor 소속범위 확인 항목 추가(병합 셀 불일치 시 경고). 오류 리포트에 자동교정 이력(원본값→교정값) 시트 신설로 감사 추적 지원. 오류 등급 항목 강제 취합 시 결과 화면에 특이사항 안내 신설. 상세는 F2_취합_엑셀_상세정의.md 참조 | F2-2, F2-3, F2-11, F2-12, §6.4, F2_취합_엑셀_상세정의.md |

### 잔여 리스크 노트
- **자유 가입 정책**: 내부용 서비스임에도 가입 제한이 없어 외부인 가입이 기술적으로 가능. Admin의 신규 가입 모니터링을 운영 절차로 반드시 수립할 것. 외부 가입 사례가 실제 발생하면 도메인 제한 도입을 재검토
- **hwpx 전환 게이트**: M1 말 PoC 판정이 '전환'으로 나올 경우 상용 SDK 라이선스 계약 기간이 M2 일정에 영향을 줄 수 있으므로, M1 중반에 후보 SDK 견적을 병행 확보해둘 것
- **모드 C 그룹 매핑 관리 방식**: 그룹 매핑(시트명→그룹명)을 화면 UI로 관리할지, 별도 설정 파일로 관리할지 design.md 단계에서 확정 필요
- **모드 D 요약 지표 정의**: 종합요약 시트에서 집계할 지표(컬럼)를 고정할지, 사용자가 매번 지정하게 할지 design.md 단계에서 확정 필요
- **F2 AI 재검증 신뢰도(v1.4 신규)**: 2차 AI(LLM) 재검증 판정은 확정적이지 않아 기본적으로 '경고' 등급으로만 처리하고 자동교정 대상에서 제외함. 실제 오탐률은 파일럿 테스트로 검증 필요
- **이미지 데이터 AI 분석(v1.4 신규)**: 이미지 셀 검토는 규칙 기반(확장자·용량·해상도·중복 등) 구조 체크로 한정하며, 내용 기반 AI Vision 분석은 v2에서 검토
- **F1 AI 직접 저작 품질 편차(v1.5 신규)**: 양식 내용을 AI가 직접 저작하므로 동일 사양에서도 실행마다 결과가 달라질 수 있음. 미리보기 필수 노출 + 대화형 수정(F1-7) 흐름으로 흡수하되, 대표 시나리오 기반 품질 테스트(골든 테스트)를 출시 전 수행할 것
- **F1 AI 주도 문답의 완료 판정(v1.5 신규)**: 사양 완료 판정을 AI가 주도하므로 조기 완료·과잉 질문 가능성 존재. 시스템의 완결성 검증과 문답 턴 수 상한으로 보강하며, 실제 판정 품질은 파일럿 테스트로 검증 필요
- **F1 이미지 첨부 개인정보(v1.5 신규)**: png 첨부는 마스킹이 적용되지 않는 유일한 외부 전송 경로. 고지 문구의 실효성을 모니터링하고, 문제 사례 발생 시 v2의 AI 기반 개인정보 탐지(Vision) 도입을 앞당겨 검토
- **F6 원본 파일 보안(v1.6 신규)**: 원본 무변형 등록 원칙상 손상되거나 매크로를 포함한 파일이 그대로 보존될 위험이 있어 매크로 포함 확장자(xlsm)를 등록 대상에서 제외함. 바이러스 스캔 연동은 v2 검토
- **F6 예시 데이터 개인정보(v1.6 신규)**: 인지 대상으로 예시(샘플) 데이터가 포함된 실제 회신 파일이 업로드될 가능성이 있어 F1보다 PII 노출 위험이 높음. 성명 마스킹 기본값을 F6에서는 "마스킹함"으로 전환하고 실제 운영 중 노출 사례를 모니터링할 것
- **F6 구조 인지 정확도(v1.6 신규)**: 헤더가 여러 행에 걸치거나 병합된 복잡한 실제 기관 양식은 구조 인지 정확도가 낮을 수 있음. 확인 질문으로 보완하되 대표 양식 샘플 기반 골든 테스트가 출시 전 필요
- **이미지 소속 범위 모호(v1.7 신규)**: 이미지 anchor가 병합 셀이 아닌 채 여러 데이터 행에 걸쳐 있으면 소속 레코드가 모호해 경고로 처리됨. 실제 발생 빈도는 파일럿 테스트로 확인 필요
- **파일 간(교차) 중복 레코드(v1.7 신규)**: 현재 F2 중복 판정은 파일 내부로 한정되어, 서로 다른 파일이 동일 레코드를 중복 제출하는 경우는 탐지되지 않음. 실무 발생 빈도를 확인한 뒤 필요 시 v2 검토 과제로 관리

---

## 11. UI 설계 참고 (화면 목업 반영)

핵심 화면 10개 전체에 대해 디자인 시안(목업)을 진행했다. 상세 비주얼과 인터랙션 명세는 design.md에서 별도 관리하고, 본 절에서는 PRD 요구사항과의 연결점만 요약한다.

| 화면 | 관련 ID | 핵심 설계 반영 사항 |
|---|---|---|
| 화면 1 · 로그인 및 회원가입 | F4-1 | 로그인 ID를 사번(직원번호)으로 변경. 회원가입 시에는 비밀번호 재설정용 이메일을 별도 필드로 추가 수집. 로그인/회원가입 탭 전환에 따라 확인 필드가 자동으로 나타나고 사라지도록 구성 |
| 화면 2 · 일반 User 홈 | F4-4, F1, F2-1 | 상단 탭바를 '생성'/'취합'으로 구성, 기본 선택 탭은 '생성'. '생성' 탭은 GPT/Gemini 홈 화면처럼 인사말 + 큰 프롬프트 입력창 + 추천 프롬프트 칩으로 구성하고, '취합' 탭 선택 시 화면 4(엑셀 업로드)로 전환. 로그인 후 모든 User가 진입하는 기본 화면 |
| 화면 3 · AI 양식 생성 | F1-1~F1-2, F1-7, **F6-1~F6-4** | 단발성 프롬프트 입력 대신 챗봇형 문답 UI로 변경 — 대상 부서·포함 항목뿐 아니라 항목별 입력 형식(날짜 표기, 단위 등)까지 PRD 인터뷰처럼 심층 질문. 우측에 지금까지 확인된 사양을 카드로 실시간 표시하고, 사양이 모두 채워지기 전에는 "생성" 버튼이 비활성 상태로 유지됨. **(v1.5)** 문답이 AI 주도(고정 단계 없음)로 변경됨에 따라 진행률은 확인된 사양 주제 기반 근사치로 표시하고, 사양 카드는 누적 방식이 아닌 매 턴 전체 갱신(항목 수정·삭제 반영). 생성 완료 후 결과 미리보기와 대화형 수정 입력창을 같은 화면에 노출. **(v1.6)** 대화창 내 사용자 의도 판정에 따라 F6(기존 양식 인지·등록) 플로우로 자동 분기 가능. 인지 모드에서는 우측 패널이 '인식된 양식 정보'로 전환되고 필드 카드에 신뢰도 배지(확인됨/확인 필요)가 추가되며, 결과 영역은 새로 저작된 워크북 미리보기 대신 원본 구조 미리보기 + 작성기준 검수 표로 대체되고 대화형 수정은 비활성화된다(작성 기준만 별도로 수정 가능). 의도가 모호하면 "새 양식 만들기"/"첨부 파일 그대로 등록" 버튼 2개를 인라인 제시 |
| 화면 4 · 엑셀 업로드 및 구조 인식 | F2-1 | 업로드 즉시 파일별로 인식된 시트 목록과 상태 표시(인식 완료/확인 필요)를 노출 |
| 화면 5 · 검토 결과 및 취합 범위 선택 | F2-3, F2-4 | 체크박스 선택/해제 시 "선택된 파일 n/m건"과 하단 진행 버튼 문구가 정적 텍스트가 아니라 실시간으로 연동되도록 수정 반영. **(v1.4)** 파일 상태 표시는 정상/이상 2종으로 단순화되며, 상태값 마우스오버·클릭 시 상세 사유(파일명·시트명·셀 위치 포함)를 노출 |
| 화면 6 · 합성 모드 선택 | F2-6~F2-9 | 모드 A/B/C/D를 카드형 선택 UI로 제시. 각 모드 설명을 완전한 문장으로 다시 작성하고, 카드 선택 시 하단에 해당 모드로 취합했을 때 생성되는 시트 탭 목록과 예시 데이터를 보여주는 "결과 미리보기" 영역을 추가 |
| 화면 7 · 취합 결과 및 다운로드 | F2-11, F2-12 | 지표 카드(취합 파일/생성 시트/자동 수정/제외 파일)에 아이콘을 적용. 결과 파일·제외 파일 카드는 반응형 2열 그리드로, 자동 수정 항목은 아이콘 기반 카드 그리드로 배치. 지표 카드 그리드는 CSS 미디어 쿼리 기준폭으로 좁은 폭에서는 2×2, 넓은 폭에서는 4×1로만 전환되도록 고정(auto-fit 사용 시 발생하는 3+1 같은 중간 배치 방지) |
| 화면 8 · 한글(hwpx) 병합 | F3-1, F3-2 | 업로드된 파일 개수를 실시간 표시하고, 목록의 각 행을 실제로 드래그하면 병합 순서가 즉시 바뀌도록 구현(정적 그립 아이콘이 아닌 동작하는 재정렬) |
| 화면 9 · 작업 이력 | F4-2 | 검색창과 유형 필터(전체/엑셀/hwpx) 클릭 시 목록이 실제로 필터링되도록 구성. 일반 User의 로그인 후 랜딩 화면 후보로도 검토 중(잔여 리스크 노트 참조) |
| 화면 10 · Admin 콘솔 | F4-3 | 지표 카드·계정 목록·정책 입력 영역의 구조와 여백을 재검토. 보관 기간 입력칸이 잘려 보이던 문제를 너비·패딩 조정으로 수정하고, 계정 이메일이 길어질 경우 말줄임 처리되도록 보완. 화면 2가 일반 User 홈으로 재정의되면서, Admin의 시스템 사용 현황 대시보드는 본 화면 하나로 통합됨 |

---

## 12. 관련 상세 스펙 문서

본 PRD는 팀 공유용 개요 문서로 유지하고, 기능별 세부 기술 스펙은 담당자별로 별도 md 문서로 분리하여 관리한다.

| 기능 | 상세 스펙 문서 | 상태 |
|---|---|---|
| F1. 생성 — AI 양식 생성 (저작 방식, 사양 스키마, 문답 제어, API 등) | `생성기능_기술명세_v0.1.md` | 문서 버전 v0.3 — 외부 양식 등록 관련 내용은 F6로 이관 완료 |
| F2. 취합 — 엑셀 (검토 아키텍처, 데이터 유형별 판정 기준 등) | `F2_취합_엑셀_상세정의.md` | 작성 중 (담당자별 업데이트 예정) |
| F3. 취합 — 한글 | (필요 시 추가 예정) | 미작성 |
| F6. 인지 — 기존 양식 인지·등록 (원본 무변형 등록, 구조 인지, 작성기준 추출) | `인지기능_기술명세_v0.1.md` | 초안 완료 |
