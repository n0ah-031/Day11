# 런타임 의존성. 버전을 고정하는 이유는 배포가 재현 가능해야 하기 때문이다 —
# 로컬에서 통과한 수트와 배포된 것이 같은 코드로 돌아야 한다.
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
httpx==0.28.1
openpyxl==3.1.5
pillow==11.1.0          # openpyxl이 이미지 anchor를 다룰 때 쓴다(§9 이미지 재배치)
openai==1.59.6
pyjwt==2.10.1
cryptography==44.0.0    # JWT ES256 서명 검증에 필요하다
